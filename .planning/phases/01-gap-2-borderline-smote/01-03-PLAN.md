---
phase: 01-gap-2-borderline-smote
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - results/train_log_v32_gap2_smote_ab.txt
  - results/smote_ab_comparison.csv
  - results/smote_borderline_counts.csv
  - results/metrics_global_standard.csv
  - results/metrics_global_borderline.csv
  - results/bootstrap_ci_standard.csv
  - results/bootstrap_ci_borderline.csv
  - results/mcnemar_vs_baselines_standard.csv
  - results/mcnemar_vs_baselines_borderline.csv
  - results/metrics_global.csv
  - results/bootstrap_ci.csv
  - results/mcnemar_vs_baselines.csv
autonomous: true
requirements: [SMOTE-04]

must_haves:
  truths:
    - "results/ contains a real, freshly executed A/B comparison between standard and borderline SMOTE, same seed/folds — not a code-review-only claim"
    - "The adopt/reject decision in the report is grounded in actual Macro F1 numbers produced by this run"
    - "The safe/borderline/noise count table reflects real per-fold/per-vowel/per-network classification outcomes, including how often the empty-pool fallback fired"
    - "The plain, unsuffixed train/full CLI path (mode_train_ex(..., NULL) branch, the code path real users/CI actually invoke) is re-run after plan 01-02's refactor and confirmed non-regressed against the Phase 0 baseline (Macro F1 ~0.4514, results/train_log_v29_baseline_reconfirmed.txt) — this is the one code path the smote-ab run never exercises, since mode_smote_ab() always passes a non-NULL ABResult*"
  artifacts:
    - path: "results/train_log_v32_gap2_smote_ab.txt"
      provides: "Full A/B report: Macro F1/per-class F1 table, direct McNemar chi2/p, explicit adopt/reject decision sentence"
      contains: "DECISAO: Borderline-SMOTE"
    - path: "results/smote_borderline_counts.csv"
      provides: "fold,vowel,network,class,safe,borderline,noise rows from the actual borderline arm run"
    - path: "results/smote_ab_comparison.csv"
      provides: "Machine-readable Macro F1 + per-class F1 + CI comparison table"
    - path: "results/metrics_global.csv"
      provides: "Regenerated plain train/full CLI output (mode_train_ex(..., NULL) branch, unsuffixed filename), confirmed consistent with the Phase 0 baseline"
  key_links:
    - from: "./build/vocal_detect smote-ab ."
      to: "results/train_log_v32_gap2_smote_ab.txt"
      via: "mode_smote_ab() -> write_smote_ab_report()"
      pattern: "DECISAO: Borderline-SMOTE"
    - from: "./build/vocal_detect train ."
      to: "results/metrics_global.csv"
      via: "mode_train() -> mode_train_ex(base_dir, SMOTE_STANDARD, NULL) -- the result == NULL branch, unexercised by smote-ab"
      pattern: "mode_train_ex"
---

<objective>
Execute the real, end-to-end A/B comparison (`./build/vocal_detect smote-ab .`) built in plans 01-01/01-02, producing durable, freshly-generated evidence in `results/` for SMOTE-04: a Macro F1 + per-class F1 comparison table with McNemar/bootstrap CI, a safe/borderline/noise count table per class/fold/vowel/network, and an explicit, code-computed adopt/reject decision. Additionally, re-run the plain, unsuffixed `train`/`full` CLI path — the `mode_train_ex(..., NULL)` branch that real users/CI actually invoke, and the one branch the `smote-ab` run never touches — to confirm plan 01-02's `mode_train()` -> `mode_train_ex()` refactor introduced no regression, before verifying and handing this phase's decision to plan 01-04 for documentation.

Purpose: This is the actual "reproducible A/B comparison rather than assumption" the milestone's core value demands — no prior plan in this phase runs the full pipeline; this is the one place where real numbers are generated. The plain-CLI regression check closes a specific scope gap: without it, nothing in this phase would ever re-verify the exact code path real users/CI invoke after the refactor, which directly serves this project's "no regression below Macro F1 0.42" constraint.
Output: 12 files in `results/` (see frontmatter), all freshly generated by this plan's runs (a plain `train` regression run plus a single run of the new `smote-ab` CLI mode), plus a verified, sanity-checked decision that plan 01-04 will document in `CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gap-2-borderline-smote/01-RESEARCH.md
@.planning/phases/01-gap-2-borderline-smote/01-02-SUMMARY.md
@results/train_log_v29_baseline_reconfirmed.txt
@results/metrics_global.csv
@CLAUDE.md

<critical_process_note>
This plan's Task 1 and Task 2 are both genuinely long-running foreground operations. Task 1
(plain `train`/`full` regression check) is a single pipeline run, ~60-90 minutes wall clock per
CLAUDE.md's documented `make train` timing. Task 2 (`smote-ab`) is ~60-180 minutes, since it
runs the entire hierarchical fusion pipeline twice in one process. Follow the exact pattern
established in Phase 0's `00-03-PLAN.md` execution (per its SUMMARY's "Background-run-and-poll
pattern") for BOTH tasks: detach the process (`nohup`/`setsid`), redirect output with `tee` (not
just `>`) so progress is visible, and actively poll to completion in-turn (e.g. via the
`run_in_background` Bash parameter plus the Monitor tool, or a `pgrep`-based wait loop) rather
than ending the turn while the process is still running. Do not fire-and-forget.
</critical_process_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression-check the plain train/full CLI path (mode_train_ex(..., NULL) branch) after the refactor</name>
  <files>none (runtime execution only)</files>
  <read_first>
    src/main.c (confirm the `mode_train()` thin wrapper — `static int mode_train(const char *base_dir) { return mode_train_ex(base_dir, SMOTE_STANDARD, NULL); }` — exists exactly as plan 01-02 specified, before starting a multi-hour run)
    .planning/phases/01-gap-2-borderline-smote/01-02-SUMMARY.md
    results/train_log_v29_baseline_reconfirmed.txt (Phase 0's reconfirmed baseline: Macro F1 ~0.4514, Accuracy ~0.6976)
  </read_first>
  <action>
Confirm prerequisites: `results/` and `models/` directories exist (`mkdir -p results models`);
`make` builds `build/vocal_detect` with zero errors. Confirm the working directory has the
gitignored SVD audio directories and `overview_merged.csv` present (per CLAUDE.md
Prerequisites) — if absent, this task cannot proceed and must be reported as blocked, not
worked around.

Run `./build/vocal_detect train . 2>&1 | tee results/train_log_v32_gap2_regression_check_console.txt`
(the `full` alias resolves to the identical `mode_train()` -> `mode_train_ex(base_dir,
SMOTE_STANDARD, NULL)` call per plan 01-02's Task 1, so either mode string exercises the same
code path) as a detached, polled background process per the critical_process_note above. Wait
for the process to exit. Confirm exit code `0`.

This specifically exercises the `result == NULL` branch of `mode_train_ex()` — the ONE code
path this plan's Task 2 (`smote-ab`) never touches, since `mode_smote_ab()` always passes a
non-NULL `ABResult*` to both of its `mode_train_ex()` calls. Confirming this path still
produces the original, unsuffixed artifact filenames (`results/metrics_global.csv`,
`results/bootstrap_ci.csv`, `results/mcnemar_vs_baselines.csv` — NOT `_standard`/`_borderline`
suffixed) with fresh modification times is the direct proof that plan 01-02's refactor is
behavior-preserving for real users/CI, who only ever invoke `train`/`full`, never `smote-ab`.

Compare the regenerated `results/metrics_global.csv`'s Macro F1 against
`results/train_log_v29_baseline_reconfirmed.txt`'s previously-recorded value (~0.4514, Accuracy
~0.6976, Phase 0's reconfirmed baseline). A moderate difference (roughly +/-0.04-0.05,
consistent with the same RNG-sequence run-to-run variance already documented in Phase 0's own
accepted decisions) is expected and NOT a bug. If the difference exceeds ~0.15, or the result
collapses toward a degenerate majority-class-only pattern, flag this explicitly as an anomaly
requiring investigation BEFORE proceeding to Task 2's much longer A/B run — do not silently
proceed if this cheaper, faster check already reveals a regression.
  </action>
  <acceptance_criteria>
    - Process exit code `0` (captured via `echo "exit:$?"` immediately after the run completes)
    - `test -s results/metrics_global.csv && echo present` outputs `present`, with a modification time after this task's run started (not a stale Phase 0/prior-phase leftover)
    - `test -s results/bootstrap_ci.csv && echo present` outputs `present`
    - `test -s results/mcnemar_vs_baselines.csv && echo present` outputs `present`
    - The three filenames above are exactly the unsuffixed names (no `_standard`/`_borderline` suffix), confirming the `result == NULL` branch's filename behavior
    - The regenerated Macro F1 is within the documented-as-expected range of the Phase 0 baseline (~0.4514 +/- 0.05-ish), or an explicit anomaly note is recorded and this task's SUMMARY flags it before Task 2 proceeds
  </acceptance_criteria>
  <verify>
    <automated>test -s results/metrics_global.csv && test -s results/bootstrap_ci.csv && test -s results/mcnemar_vs_baselines.csv</automated>
  </verify>
  <done>The plain, unsuffixed `train`/`full` CLI path (mode_train_ex(..., NULL) branch) runs to completion post-refactor, produces the original unsuffixed artifact filenames, and its Macro F1 is confirmed consistent with (not regressed from) Phase 0's reconfirmed baseline — this is the code path real users/CI actually invoke, and plan 01-02's `smote-ab` mode never exercises it.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full A/B pipeline (smote-ab CLI mode) to completion</name>
  <files>none (runtime execution only)</files>
  <read_first>
    src/main.c (confirm mode_smote_ab()/write_smote_ab_report()/smote-ab dispatch exist and match plan 01-02's SUMMARY before starting a multi-hour run)
    .planning/phases/01-gap-2-borderline-smote/01-02-SUMMARY.md
  </read_first>
  <action>
Confirm prerequisites: `results/` and `models/` directories exist (`mkdir -p results models`);
`make` builds `build/vocal_detect` with zero errors. Confirm the working directory has the
gitignored SVD audio directories and `overview_merged.csv` present (per CLAUDE.md
Prerequisites) — if absent, this task cannot proceed and must be reported as blocked, not
worked around.

Run `./build/vocal_detect smote-ab . 2>&1 | tee results/train_log_v32_gap2_smote_ab_console.txt`
as a detached, polled background process per the critical_process_note above. Wait for the
process to exit. Confirm exit code `0`.

After completion, confirm all 9 A/B-specific files listed in this plan's `files_modified`
frontmatter (excluding the 3 unsuffixed files already regenerated by Task 1) exist and have a
modification time after this task's run started (i.e., were freshly regenerated by this run,
not stale leftovers from Task 1 or Phase 0).
  </action>
  <acceptance_criteria>
    - Process exit code `0` (captured via `echo "exit:$?"` immediately after the run completes)
    - `test -s results/train_log_v32_gap2_smote_ab.txt && echo present` outputs `present`
    - `grep -c "DECISAO: Borderline-SMOTE" results/train_log_v32_gap2_smote_ab.txt` outputs `1`
    - `head -1 results/smote_borderline_counts.csv` outputs exactly `fold,vowel,network,class,safe,borderline,noise`
    - `wc -l < results/smote_borderline_counts.csv` outputs a number greater than `1` (at least one data row beyond the header)
    - `head -1 results/smote_ab_comparison.csv` outputs exactly `metric,standard,standard_ci_lower,standard_ci_upper,borderline,borderline_ci_lower,borderline_ci_upper`
    - `wc -l < results/smote_ab_comparison.csv` outputs `8` (header + 7 metric rows)
    - `for f in results/metrics_global_standard.csv results/metrics_global_borderline.csv results/bootstrap_ci_standard.csv results/bootstrap_ci_borderline.csv results/mcnemar_vs_baselines_standard.csv results/mcnemar_vs_baselines_borderline.csv; do test -s "$f" || echo "MISSING:$f"; done` outputs nothing (all 6 files present and non-empty)
  </acceptance_criteria>
  <verify>
    <automated>test -s results/train_log_v32_gap2_smote_ab.txt && grep -c 'DECISAO: Borderline-SMOTE' results/train_log_v32_gap2_smote_ab.txt</automated>
  </verify>
  <done>./build/vocal_detect smote-ab . ran to completion (exit 0) and produced all 9 expected A/B artifacts in results/, each freshly generated by this run.</done>
</task>

<task type="auto">
  <name>Task 3: Verify statistical rigor and internal consistency of the produced A/B report</name>
  <files>none (verification/inspection only)</files>
  <read_first>
    results/train_log_v32_gap2_smote_ab.txt
    results/smote_ab_comparison.csv
    results/smote_borderline_counts.csv
    results/train_log_v29_baseline_reconfirmed.txt (Phase 0's reference baseline, for the sanity cross-check below)
  </read_first>
  <action>
Extract the `standard` and `borderline` `macro_f1` values from `results/smote_ab_comparison.csv`
and confirm they are consistent with the `DECISAO:` sentence in
`results/train_log_v32_gap2_smote_ab.txt` (i.e., if `borderline >= standard`, the decision
sentence must say `ADOTADO`; otherwise it must say `REJEITADO` — this is a mechanical
cross-check of the fixed rule from plan 01-02, not a new judgment call).

Sanity-check the `standard` arm's `macro_f1` (from Task 2's `smote-ab` run) against
`results/train_log_v29_baseline_reconfirmed.txt`'s previously-established Macro F1 (~0.4514, per
Phase 0's 00-03-SUMMARY.md), and cross-reference it against Task 1's plain-CLI regression-check
result for consistency between the two independent runs. A moderate difference (roughly within
the bootstrap CI width already observed in Phase 0, e.g. +/-0.04-0.05) is expected and NOT a
bug — Phase 0's own accepted decisions already document two sources of run-to-run RNG-sequence
variation (the baseline classifiers' RNG draws shifting subsequent SMOTE/dropout sequences, and
general fold-to-fold variance). If the `standard` arm's macro_f1 is dramatically different (e.g.
differs by more than ~0.15, or collapses toward a degenerate majority-class-only result), flag
this explicitly as an anomaly requiring investigation rather than silently accepting it — this
is a scientific-integrity check, not a formality.

Inspect `results/smote_borderline_counts.csv` and count how many rows have `borderline == 0`
(the empty-borderline-pool fallback condition) — report this count and which
classes/folds/vowels/networks it concentrates in (per RESEARCH.md Pitfall 3's prediction, this
is expected to concentrate in the smallest classes, e.g. Edema de Reinke / Disfonia
Psicogenica, and is a documented limitation, not a bug).

Record the final verdict (macro_f1 for both arms, delta, McNemar chi2/p, ADOTADO/REJEITADO,
empty-pool-fallback frequency summary, and Task 1's plain-CLI regression-check outcome)
explicitly in this plan's own SUMMARY.md so plan 01-04 can consume it directly without
re-deriving anything.
  </action>
  <acceptance_criteria>
    - The `ADOTADO`/`REJEITADO` string in `results/train_log_v32_gap2_smote_ab.txt` matches the sign of `(borderline_macro_f1 - standard_macro_f1)` read from `results/smote_ab_comparison.csv` (manual cross-check, reported in the SUMMARY)
    - The standard arm's macro_f1 is either within a documented-as-expected range of the Phase 0 baseline (~0.4514 +/- 0.05-ish) or an explicit anomaly note is written explaining the divergence
    - A count of empty-borderline-pool fallback rows (`borderline == 0` in `results/smote_borderline_counts.csv`) is computed and reported
    - Task 1's plain-CLI regression-check outcome is cross-referenced and included in the final verdict
  </acceptance_criteria>
  <verify>
    <automated>grep -o 'DECISAO: Borderline-SMOTE [A-Z]*' results/train_log_v32_gap2_smote_ab.txt</automated>
  </verify>
  <done>The A/B report's decision is mechanically verified as internally consistent with its own underlying numbers; the standard arm is sanity-checked against Phase 0's baseline and against Task 1's independent plain-CLI regression run; the empty-pool fallback frequency is quantified; a complete, unambiguous verdict is recorded in this plan's SUMMARY for plan 01-04 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Long-running foreground process -> conversation turn boundary | Risk of the process being silently interrupted/abandoned across turns if not actively polled (Phase 0's 00-01 plan hit this exact issue) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service (self-inflicted, process abandonment) | The `train` regression-check run and the `smote-ab` background run | mitigate | Follow the exact `nohup`/`tee`/poll-to-completion pattern already proven in Phase 0's 00-01 and 00-03 plans, for both Task 1 and Task 2 — do not end the turn while either process is running |
| T-01-08 | Repudiation (unverifiable/cherry-picked results) | The A/B decision sentence | mitigate | Task 3 mechanically cross-checks the decision string against the raw CSV numbers rather than trusting the log file's prose at face value |
| T-01-SC | Tampering (supply chain) | N/A | accept | No package-manager installs in this phase |

</threat_model>

<verification>
`./build/vocal_detect train .` (Task 1) and `./build/vocal_detect smote-ab .` (Task 2) both exit 0 and produce all 12 files in `results/` listed in this plan's frontmatter. The decision sentence is mechanically consistent with the underlying Macro F1 numbers. Both the plain-CLI arm (Task 1) and the smote-ab standard arm (Task 2) are sanity-checked against Phase 0's reconfirmed baseline.
</verification>

<success_criteria>
- A real, freshly executed A/B comparison exists in `results/`, not a claim.
- The plain, unsuffixed `train`/`full` CLI path (mode_train_ex(..., NULL) branch) is confirmed non-regressed post-refactor — the one code path the smote-ab run never exercises.
- The adopt/reject decision is verified consistent with its own underlying numbers.
- Any anomaly (unexpected baseline divergence, high empty-pool-fallback frequency) is explicitly surfaced, not silently absorbed.
</success_criteria>

<output>
Create `.planning/phases/01-gap-2-borderline-smote/01-03-SUMMARY.md` when done, including the final verdict (both arms' Macro F1, delta, McNemar chi2/p, ADOTADO/REJEITADO, empty-pool-fallback frequency, and Task 1's plain-CLI regression-check outcome) so plan 01-04 can consume it directly.
</output>
</output>
