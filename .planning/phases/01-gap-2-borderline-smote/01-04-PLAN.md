---
phase: 01-gap-2-borderline-smote
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: [CLAUDE.md]
autonomous: true
requirements: [SMOTE-05]

must_haves:
  truths:
    - "CLAUDE.md accurately reflects the real, tested Gap 2 (Borderline-SMOTE) outcome, whichever mode was adopted"
    - "No text in CLAUDE.md implies Borderline-SMOTE was validated before this phase's real A/B run existed"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected 'What Worked'/'What Doesn't Work' bullet + a new dated Gap 2 Outcome subsection with concrete Macro F1/McNemar numbers and file pointers"
      contains: "Gap 2 Outcome"
  key_links:
    - from: "CLAUDE.md Gap 2 Outcome subsection"
      to: "results/train_log_v32_gap2_smote_ab.txt"
      via: "file-path reference"
      pattern: "train_log_v32_gap2_smote_ab.txt"
---

<objective>
Correct `CLAUDE.md`'s pre-existing documentation drift (it currently lists "Borderline-SMOTE" under "What works" despite no Borderline-SMOTE implementation having existed before this phase) and add a dated, evidence-backed Gap 2 Outcome section reflecting the real A/B decision from plan 01-03 — regardless of which mode was adopted.

Purpose: SMOTE-05 explicitly requires this update "independente do resultado" (regardless of outcome) — this is the project's own stated anti-pattern-avoidance discipline (never claim a technique works without a real A/B result backing it), and directly closes the exact documentation mistake already made once with Borderline-SMOTE (as flagged by RESEARCH.md's State of the Art section).
Output: `CLAUDE.md` updated with corrected constraint text and a new subsection; no other file touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gap-2-borderline-smote/01-RESEARCH.md
@.planning/phases/01-gap-2-borderline-smote/01-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with the Gap 2 (Borderline-SMOTE) A/B outcome</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (full file — it is short, read in full)
    .planning/phases/01-gap-2-borderline-smote/01-03-SUMMARY.md (the verdict: both arms' Macro F1, delta, McNemar chi2/p, ADOTADO/REJEITADO, empty-pool-fallback frequency)
    .planning/phases/01-gap-2-borderline-smote/01-RESEARCH.md (State of the Art section — the exact drift being corrected)
  </read_first>
  <action>
Locate the "Important Constraints" section of `CLAUDE.md`, which currently contains a
`**What works**:` line listing `Borderline-SMOTE` among its comma-separated items, and a
`**What doesn't work** (do not re-attempt):` line. Using the real verdict recorded in
`01-03-SUMMARY.md`, apply exactly one of the following two edits (do not apply both, do not
guess — use the actual recorded verdict):

If Gap 2's decision was ADOTADO (borderline Macro F1 >= standard): edit the `**What works**:`
line so the bare word `Borderline-SMOTE` is replaced with a citation-precise, evidence-backed
phrase, e.g. `Borderline-SMOTE1 (Han/Wang/Mao 2005, validated by reproducible A/B in v32 --
see results/train_log_v32_gap2_smote_ab.txt)` — do not just leave the old bare claim
unchanged, since it previously had zero backing evidence and now has real evidence that should
be cited precisely.

If Gap 2's decision was REJEITADO (borderline Macro F1 < standard): REMOVE the bare
`Borderline-SMOTE` item from the `**What works**:` line entirely, and ADD a new item to the
`**What doesn't work** (do not re-attempt):` line, e.g. `Borderline-SMOTE1 (Han/Wang/Mao 2005)
-- implemented and A/B tested in v32 (results/train_log_v32_gap2_smote_ab.txt): Macro F1
{borderline_value} < {standard_value} padrao (delta {delta_value}) -- standard SMOTE retained
in production`.

In either case, add a new subsection immediately after the "Important Constraints" section
(before "Output Files"), titled `## Gap 2 Outcome — Borderline-SMOTE A/B (v32)`, containing:
the exact `DECISAO:` sentence copied verbatim from `results/train_log_v32_gap2_smote_ab.txt`;
a two-row comparison (standard vs. borderline: accuracy, macro_f1, per-class F1) sourced from
`01-03-SUMMARY.md`'s recorded verdict; the direct McNemar chi2/p between the two arms; a
one-line summary of the safe/borderline/noise fallback frequency (from `01-03-SUMMARY.md`);
and explicit file pointers to `results/train_log_v32_gap2_smote_ab.txt`,
`results/smote_ab_comparison.csv`, and `results/smote_borderline_counts.csv`. Do not alter any
other section of `CLAUDE.md` (Build & Run Commands, Architecture, Current Best Results, etc.
stay untouched by this plan).
  </action>
  <acceptance_criteria>
    - `grep -c "## Gap 2 Outcome" CLAUDE.md` outputs `1`
    - `grep -c "train_log_v32_gap2_smote_ab.txt" CLAUDE.md` outputs at least `1`
    - `grep -c "DECISAO: Borderline-SMOTE" CLAUDE.md` outputs at least `1`
    - If REJEITADO: `grep "What works" CLAUDE.md | grep -c "Borderline-SMOTE"` outputs `0` (the old bare, unbacked claim is gone from What Works)
    - If ADOTADO: `grep "What works" CLAUDE.md | grep -c "Han/Wang/Mao 2005"` outputs `1` (the claim is now citation-precise, not bare)
    - `git diff --stat CLAUDE.md` shows exactly 1 file changed, with additions/edits scoped to the "Important Constraints" bullet and the new "Gap 2 Outcome" subsection (no unrelated sections touched)
  </acceptance_criteria>
  <verify>
    <automated>grep -c '## Gap 2 Outcome' CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md's What Works/What Doesn't Work claims about Borderline-SMOTE accurately reflect the real A/B result (adopted or rejected); a new, dated, evidence-backed Gap 2 Outcome subsection exists with the decision sentence, comparison numbers, McNemar result, and file pointers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documentation content -> future readers (PIBIC committee, future contributors, future Claude sessions) | Inaccurate documentation here directly causes the exact repeat mistake this phase exists to fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Repudiation (false/unbacked claim in documentation) | `CLAUDE.md`'s What Works/What Doesn't Work bullets | mitigate | This task requires the edit to be sourced directly from `01-03-SUMMARY.md`'s recorded verdict (itself sourced from a real, freshly executed run in plan 01-03) — never asserted independently of that evidence chain |
| T-01-SC | Tampering (supply chain) | N/A | accept | No package-manager installs in this phase; this is a documentation-only plan |

</threat_model>

<verification>
`grep -c "## Gap 2 Outcome" CLAUDE.md` == 1. The What Works/What Doesn't Work bullet correctly reflects the actual recorded verdict (ADOTADO keeps a citation-precise "works" claim; REJEITADO moves it to "doesn't work" with numbers). `git diff CLAUDE.md` touches only the intended sections.
</verification>

<success_criteria>
- CLAUDE.md's Borderline-SMOTE claim is accurate and evidence-backed, regardless of which way the A/B decision went.
- A dated Gap 2 Outcome subsection exists with concrete numbers and file pointers, satisfying SMOTE-05.
</success_criteria>

<output>
Create `.planning/phases/01-gap-2-borderline-smote/01-04-SUMMARY.md` when done
</output>
