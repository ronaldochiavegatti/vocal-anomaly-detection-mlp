---
phase: 01-gap-2-borderline-smote
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/main.c]
autonomous: true
requirements: [SMOTE-01, SMOTE-02, SMOTE-03]

must_haves:
  truths:
    - "smote_oversample() behavior differs between SMOTE_STANDARD and SMOTE_BORDERLINE modes: borderline mode restricts the synthesis base-point pool to samples classified BORDERLINE (or falls back to the full class pool with a warning if none exist)"
    - "Safe/borderline/noise classification never truncates the 2*m>=k boundary via integer division; the m==k NOISE case is checked before the 2*m>=k BORDERLINE case"
    - "Same-class find_knn() interpolation never consumes the all-class find_knn_global() neighbor list — the two searches remain textually distinct function calls"
    - "An empty borderline pool, and the pre-existing n_class<=1 case, both log an explicit warning; the n_class<=1 case skips synthesis entirely (produces zero synthetic samples for that class) rather than crashing or producing a degenerate duplicate"
  artifacts:
    - path: "src/main.c"
      provides: "SmoteMode enum, SmoteBorderlineCounts struct, find_knn_global(), classify_borderline(), modified smote_oversample() signature and body"
      contains: "static int find_knn_global"
  key_links:
    - from: "smote_oversample() per-class classification block"
      to: "find_knn_global()"
      via: "direct function call over the all-class candidate set (n_in samples)"
      pattern: "find_knn_global\\(x_in, class_idx\\[c\\]\\[i\\]"
    - from: "smote_oversample() synthesis loop"
      to: "find_knn() (unchanged, same-class only)"
      via: "interpolation neighbor search restricted to class_idx[c]/n_class, never the borderline pool"
      pattern: "find_knn\\(x_in, base_idx, class_idx\\[c\\], n_class"
---

<objective>
Implement the Borderline-SMOTE1 (Han, Wang & Mao, 2005) classification/generation core as an in-place extension of the existing `smote_oversample()` function in `src/main.c`: a new `SmoteMode` enum, a new `find_knn_global()` all-class k-NN search, a new `classify_borderline()` safe/borderline/noise classifier using the untruncated `2*m >= k` rule, and the wiring inside `smote_oversample()` that uses these to restrict the synthesis base-point pool in borderline mode — while leaving same-class interpolation via the existing `find_knn()` completely untouched.

Purpose: This is the algorithmic heart of Gap 2 (SMOTE-01, SMOTE-02, SMOTE-03) — everything else in this phase (CLI orchestration, A/B execution, documentation) depends on this code being correct and behavior-preserving for the existing `SMOTE_STANDARD` path.
Output: `src/main.c` compiles cleanly with the new mode-aware `smote_oversample()`; both existing call sites explicitly pass `SMOTE_STANDARD, NULL` (zero behavior change yet — mode threading through `mode_train()` happens in the next plan, 01-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gap-2-borderline-smote/01-RESEARCH.md
@.planning/phases/01-gap-2-borderline-smote/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Exact current state of src/main.c's SMOTE section (lines 187-231), read in full this
     session. This is the ONLY source of truth for what exists today — do not assume any
     other structure. Both call sites (inside mode_train(), currently ~lines 318 and ~335)
     are shown for the exact strings this plan's Task 2 must match/replace. -->

Existing find_knn() (src/main.c:189-203) — UNCHANGED by this plan, reused as the interpolation
search in Task 2. Signature: `static void find_knn(const float *x, int base, const int *class_indices, int n_class, int nf, int k, int *neighbors)`. Uses a partial-selection-sort over the
first `k` slots, sentinel `dists[i] = 1e30f` when `class_indices[i] == base`, allocates with
`safe_malloc`, frees `dists`/`order` before returning, has no return value.

Existing smote_oversample() (src/main.c:205-231) — the exact function this plan modifies in
place. Current signature: `static void smote_oversample(const float *x_in, const int *y_in,
int n_in, int nf, int num_classes, float **x_out, int **y_out, int *n_out)`. Local `int k = 5;`
at function top. `*n_out = max_count * num_classes;` is set ONCE, BEFORE the per-class loop, as
a pre-allocation upper bound for `*x_out`/`*y_out` — it is never corrected afterward today,
because today every class always produces exactly `n_synthetic = max_count - n_class` synthetic
rows, so `out_idx` (the running write cursor) always reaches exactly `max_count * num_classes`
by the end. Builds `class_idx[num_classes]` (index arrays per class) and `class_pos`. Per-class
loop copies originals then runs a synthesis loop of `n_synthetic = max_count - n_class`
iterations, each drawing `base_idx = class_idx[c][rng_int(n_class)]`, calling
`find_knn(x_in, base_idx, class_idx[c], n_class, nf, knn, neighbors)`, then
`neighbor_idx = neighbors[rng_int(knn)]`, `alpha = rng_uniform()`, and linear interpolation.

Existing call sites inside mode_train() (src/main.c, both currently unchanged apart from this
plan's Task 2 edit):
- Master (2-class): `smote_oversample(tr_x_v, tr_y_bin, n_train_aug, nf_vowel, 2, &os_m_x, &os_m_y, &os_n_m);`
- Expert (4-class): `smote_oversample(ex_tr_x, ex_tr_y, n_ex_tr, nf_vowel, 4, &os_e_x, &os_e_y, &os_n_e);`

Logging convention (include/utils.h): `void log_warn(const char *fmt, ...);` — printf-style,
Portuguese messages throughout this codebase.

Allocation convention (include/utils.h): `safe_malloc`/`safe_calloc`/`safe_realloc` — never
raw malloc/calloc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SmoteMode, SmoteBorderlineCounts, find_knn_global(), classify_borderline()</name>
  <files>src/main.c</files>
  <read_first>
    src/main.c (lines 1-30 for includes/section banners, and lines 187-231 for the current SMOTE section — read the whole file once, it is 485 lines, well within a single-pass budget)
    .planning/phases/01-gap-2-borderline-smote/01-RESEARCH.md (Architecture Patterns > Pattern 1; Common Pitfalls 1 and 4)
    .planning/phases/01-gap-2-borderline-smote/01-PATTERNS.md ("find_knn_global()" and "classify_borderline()" sections)
  </read_first>
  <action>
Note on source fidelity: the code sketches in `01-RESEARCH.md` (Architecture Patterns > Pattern 1)
and `01-PATTERNS.md` are illustrative pseudocode only — they demonstrate structure and algorithm
logic, not literal text to commit. Any English identifiers or inline comments appearing in those
sketches (e.g. `/* NOISE */`, `/* BORDERLINE (danger) */`, `/* SAFE */`) MUST be translated to
Portuguese in the actual code committed to `src/main.c`, per `CLAUDE.md`'s Comments convention
(all prose comments are in Portuguese, `/* ... */` block-comment style only — `//` is never
used). Code identifiers themselves (function/variable names like `find_knn_global`,
`classify_borderline`, `neighbors`) stay in English per the codebase's existing naming
convention — only prose comments must be Portuguese.

Insert new code immediately after the existing `find_knn()` function (src/main.c:189-203) and
before `smote_oversample()` (src/main.c:205), inside the `/* ========== SMOTE ========== */`
section. Add, in this order:

1. `typedef enum { SMOTE_STANDARD = 0, SMOTE_BORDERLINE = 1 } SmoteMode;` — module-level enum.
2. Two local `#define` constants (per Open Question 3's resolution in 01-RESEARCH.md: kept
   local to main.c, not promoted to config.h, matching the existing local `int k = 5;`
   convention already inside `smote_oversample()`): `#define SMOTE_K_NEIGHBORS 5` and
   `#define BORDERLINE_M_NEIGHBORS 5`. Also add `#define MAX_SMOTE_CLASSES 4` — this function
   is only ever called with `num_classes` 2 (Master, binary) or 4 (Expert, 4-class), never the
   full `NUM_CLASSES` (5), so 4 is the correct, tight bound for count-tracking arrays.
3. `SmoteBorderlineCounts` struct: three `int` arrays of size `MAX_SMOTE_CLASSES` named
   `safe`, `borderline`, and `noise` — a flat, plain-data struct (no methods), matching this
   codebase's `ConfidenceInterval`/`MetricsResult` style (include/metrics.h).
4. `find_knn_global()`: a new `static int` function with signature
   `static int find_knn_global(const float *x, int base, int n_in, int nf, int k, int *neighbors)`.
   Copy `find_knn()`'s exact partial-selection-sort body structure, but search over ALL `n_in`
   samples (indices 0..n_in-1 directly, not through a `class_indices[]` restriction array) —
   this is the "all-class" search that Borderline-SMOTE1's classification step requires. Use
   the same `dists[i] = 1e30f` sentinel when `i == base` (never let the base point select
   itself). Allocate `dists`/`order` with `safe_malloc(n_in * sizeof(...))`. Clamp
   `int kk = (k < n_in - 1) ? k : n_in - 1; if (kk < 1) kk = 1;` before the selection-sort loop
   (mirrors `find_knn()`'s own `knn` clamp computed by its caller, but here clamped inside the
   function since `find_knn_global()` owns its own `k`). Free `dists`/`order` before returning.
   Return `kk` — the actual neighbor count found (may be less than the nominal `k` for tiny
   `n_in`). Add an inline comment (Portuguese) directly above the function stating it must call
   zero `rng_*` functions (RESEARCH.md Pitfall 4 — this preserves RNG-draw-count parity between
   `SMOTE_STANDARD` and `SMOTE_BORDERLINE`) and must never be parallelized with
   `#pragma omp parallel for` unless this "zero rng_* calls" invariant is re-verified first,
   mirroring the existing warning comment style already present above
   `precalculate_augmentations()` (that existing comment is itself in Portuguese — match its
   tone/style, do not introduce an English comment here).
5. `classify_borderline()`: a new `static int` function, signature
   `static int classify_borderline(int m, int k)`. Body, in this exact branch order (order is
   load-bearing per Pitfall 1 — `2*m >= k` is also true when `m == k`, so the `m == k` check
   must come first): first branch `if (m == k) return 2;` with an inline Portuguese comment
   `/* RUIDO */`; second branch `if (2 * m >= k) return 1;` with an inline Portuguese comment
   `/* BORDERLINE (perigo) */` — explicitly using `2 * m >= k`, never `m >= k / 2`
   (integer-division truncation would silently shift the boundary for k=5, per Pitfall 1);
   final `return 0;` with comment `/* SEGURO */`. All three inline comments MUST be Portuguese
   (`RUIDO`, `BORDERLINE (perigo)`, `SEGURO`, or equivalent consistent Portuguese terms) — do
   not use the English words `NOISE`/`SAFE`/`danger` that appear in the illustrative research
   sketches. No logging inside this pure classifier (logging happens at the call site, added in
   Task 2, per SMOTE-03).

Do not modify `smote_oversample()`'s signature or body in this task — that is Task 2. Do not
modify the two call sites in `mode_train()` in this task. This task only adds new, currently
unused (from the compiler's perspective, until Task 2 wires them in) helper code; if the
compiler warns about unused static functions at the end of this task, that is expected and
temporary — do NOT suppress it with attributes, since Task 2 (same plan) immediately
eliminates the warning by calling both new functions.
  </action>
  <acceptance_criteria>
    - `grep -c "typedef enum { SMOTE_STANDARD = 0, SMOTE_BORDERLINE = 1 } SmoteMode;" src/main.c` outputs `1`
    - `grep -c "^static int find_knn_global" src/main.c` outputs `1`
    - `grep -c "^static int classify_borderline" src/main.c` outputs `1`
    - `grep -n "m == k" src/main.c | head -1` reports a lower line number than `grep -n "2 \* m >= k" src/main.c | head -1` (branch order verification)
    - `awk '/^static int find_knn_global/,/^}/' src/main.c | grep -c "rng_"` outputs `0` (zero RNG calls inside the function body)
    - `grep -c "SmoteBorderlineCounts" src/main.c` outputs at least `1` (struct definition present)
    - `grep -c "/\* NOISE \*/\|/\* SAFE \*/\|(danger)" src/main.c` outputs `0` (no English classifier comments committed — Portuguese only)
    - `make 2>&1 | grep -c "error:"` outputs `0`
  </acceptance_criteria>
  <verify>
    <automated>make 2>&1 | grep -c 'error:'</automated>
  </verify>
  <done>find_knn_global() and classify_borderline() exist as standalone, correct, RNG-free functions with fully Portuguese inline comments; SmoteMode and SmoteBorderlineCounts are defined; the file still compiles (possibly with an expected transient "unused function" warning that Task 2 resolves in this same plan).</done>
</task>

<task type="auto">
  <name>Task 2: Wire SmoteMode + borderline classification into smote_oversample(), add SMOTE-03 fallback warnings</name>
  <files>src/main.c</files>
  <read_first>
    src/main.c (re-read the current `smote_oversample()` body and both call sites after Task 1's insertion — line numbers will have shifted by Task 1's addition)
    .planning/phases/01-gap-2-borderline-smote/01-RESEARCH.md (Common Pitfalls 2, 3, 4; Open Questions 1)
    .planning/phases/01-gap-2-borderline-smote/01-PATTERNS.md ("smote_oversample() (service, modified in place)" — the exact insertion-point comments A and B)
  </read_first>
  <action>
Modify `smote_oversample()`'s signature to add two new parameters between `num_classes` and
the output parameters: `SmoteMode smote_mode` and `SmoteBorderlineCounts *counts` (nullable —
pass `NULL` when the caller does not need per-class safe/borderline/noise counts, which is
every call site until plan 01-02 threads real counts through). New signature:
`static void smote_oversample(const float *x_in, const int *y_in, int n_in, int nf,
int num_classes, SmoteMode smote_mode, SmoteBorderlineCounts *counts, float **x_out,
int **y_out, int *n_out)`.

Replace the local `int k = 5;` with `int k = SMOTE_K_NEIGHBORS;` (uses Task 1's new constant).

Inside the per-class loop (`for (int c = 0; c < num_classes; c++) { ... }`), after computing
`n_class = counts[c]` and BEFORE the existing synthesis loop (`for (int s = 0; s < n_synthetic; s++)`),
insert the following two independent guard/setup blocks (all new log messages and comments in
this block must be Portuguese, per CLAUDE.md's Comments convention — matching Task 1's
requirement):

Block A — pre-existing `n_class<=1` degeneration (SMOTE-03; Open Question 1 in
01-RESEARCH.md was RE-RESOLVED, superseding the plan's original interpretation (a), by an
explicit user decision made after the second verification pass: option (b) — SKIP synthesis
entirely — is the adopted fix, not "log and continue producing the duplicate". A logged
duplicate is still a degenerate synthetic sample; ROADMAP.md/REQUIREMENTS.md's SMOTE-03 wording
("no degenerate synthetic samples") is only literally satisfied by producing zero synthetic
rows for that class, not a documented one): immediately after the existing line
`int n_synthetic = max_count - n_class;` and BEFORE the `int *neighbors = (int *)safe_malloc(knn * sizeof(int));`
allocation, insert:
`if (n_class <= 1) { if (n_synthetic > 0) { log_warn("smote_oversample: classe %d com apenas %d amostra(s) -- sintese pulada (nenhum par valido da mesma classe para interpolar, zero amostras sinteticas geradas para esta classe)", c, n_class); } n_synthetic = 0; }`.
Setting `n_synthetic = 0` (rather than leaving the pre-computed `max_count - n_class` value in
place) is what makes this a true skip, not a cosmetic warning: the existing synthesis loop
`for (int s = 0; s < n_synthetic; s++) { ... }` immediately below now executes zero iterations
for this class, so `find_knn()`/the interpolation math is never invoked at all — no duplicate,
degenerate or otherwise, is ever constructed. Only warn when `n_synthetic > 0` (this class
actually needed synthesis to reach `max_count`); this keeps the log honest and avoids a
spurious warning for a class that happens to satisfy `n_class <= 1` while already being the
class that defines `max_count` itself (an edge case that cannot occur in practice given how
`max_count` is computed, but the guard costs nothing and keeps the condition self-consistent).

Because a skipped class now contributes fewer rows to `*x_out`/`*y_out` than the `max_count *
num_classes` capacity pre-allocated for the whole matrix (the existing line
`*n_out = max_count * num_classes;`, set BEFORE the per-class loop, is a pre-allocation upper
bound, not a promise that every row will be written), `smote_oversample()`'s final `*n_out`
value must be corrected to reflect the ACTUAL number of rows written. Add, as the very last
statement change in this task, immediately before the function's existing final line
`free(class_idx); free(class_pos); free(counts);`: `*n_out = out_idx;`. This is required
correctness plumbing for the skip behavior — without it, callers (`mlp_train()`, invoked with
`os_n_m`/`os_n_e` from `mode_train()`) would be told the buffer holds `max_count * num_classes`
rows when it may hold fewer, and would read uninitialized `safe_malloc` memory for the missing
trailing rows whenever any class hits this skip path. This change is a no-op whenever no class
is skipped (today's `SMOTE_STANDARD` behavior, and any `SMOTE_BORDERLINE` run where no class
hits `n_class <= 1`): in that case `out_idx` already equals `max_count * num_classes` exactly by
construction, so `*n_out = out_idx;` assigns the same value `*n_out` already held.

Block B — borderline pool construction (only when `smote_mode == SMOTE_BORDERLINE` AND
`n_synthetic > 0` — a class already skipped by Block A needs no borderline pool at all;
building one, and possibly logging its own empty-pool warning, would be wasted work and a
second, confusing warning for a class whose fate is already decided. This keeps Block A and
Block B's fallback semantics aligned: Block A's skip takes precedence, and Block B only ever
runs for classes that will actually synthesize something): allocate
`int *class_idx_borderline = (int *)safe_malloc(n_class * sizeof(int)); int n_borderline = 0;`
then `int neighbors_g[BORDERLINE_M_NEIGHBORS];` (or a `safe_malloc`'d buffer sized
`BORDERLINE_M_NEIGHBORS` — a fixed-size stack array is fine since the size is a compile-time
constant). For each `i` in `[0, n_class)`: call
`int kk = find_knn_global(x_in, class_idx[c][i], n_in, nf, BORDERLINE_M_NEIGHBORS, neighbors_g);`,
then count `int m = 0; for (int j = 0; j < kk; j++) if (y_in[neighbors_g[j]] != c) m++;`
(neighbors NOT belonging to class `c`), then `int cls = classify_borderline(m, kk);`. If
`counts != NULL`: increment `counts->safe[c]`/`counts->borderline[c]`/`counts->noise[c]`
according to `cls` (0/1/2 respectively). If `cls == 1` (BORDERLINE), append
`class_idx[c][i]` to `class_idx_borderline[n_borderline++]`. After this loop: if
`n_borderline == 0`, call
`log_warn("smote_oversample: classe %d sem amostras borderline (fold/vogal atual) -- usando class_idx[c] completo como fallback", c);` — this is SMOTE-03's first clause (empty borderline
pool fallback), and per RESEARCH.md's Anti-Patterns section, this fallback is a project-specific
engineering extension, NOT part of Han/Wang/Mao (2005) — do not attribute it to the cited
authors in any comment or log message. Note this fallback pool (`class_idx[c]`, sized
`n_class >= 2` at this point since Block A already skipped the `n_class <= 1` case before this
block ever runs) does NOT reintroduce Pitfall 3's compounded-degeneration risk — interpolating
between two or more genuinely distinct same-class points is ordinary, non-degenerate SMOTE
behavior, not a duplicate.

Determine the synthesis pool once per class, before the synthesis loop: if
`smote_mode == SMOTE_BORDERLINE && n_borderline > 0`, the pool is `class_idx_borderline` sized
`n_borderline`; otherwise (SMOTE_STANDARD, or SMOTE_BORDERLINE with an empty pool) the pool is
the existing `class_idx[c]` sized `n_class` — this is the ONLY thing that changes about the
synthesis loop. Change the existing line
`int base_idx = class_idx[c][rng_int(n_class)];` to draw from this pool variable instead (e.g.
`int base_idx = pool[rng_int(pool_size)];`), while the immediately following line
`find_knn(x_in, base_idx, class_idx[c], n_class, nf, knn, neighbors);` MUST remain textually
unchanged — always searching `class_idx[c]`/`n_class` (SMOTE-02: interpolation neighbors are
always same-class, never the borderline pool, never the find_knn_global() result). This is
the single most important invariant in this task (RESEARCH.md Pitfall 2) — do not let
`find_knn()`'s candidate-set arguments reference `class_idx_borderline`/`n_borderline` under
any circumstance.

Critical invariant (Pitfall 4): do not add any `rng_*` call inside Block A or Block B. The
number of `rng_int()`/`rng_uniform()` calls per synthesis iteration must stay identical
between `SMOTE_STANDARD` and `SMOTE_BORDERLINE` — only which array `rng_int(pool_size)`
indexes into changes. (This invariant is unaffected by the skip fix — a skipped class simply
runs the synthesis loop zero times, consuming zero `rng_*` draws for that class in either mode,
which is itself symmetric between modes since Block A runs before the mode check.)

At the end of each class's loop body, free `class_idx_borderline` if it was allocated (guard
with `if (smote_mode == SMOTE_BORDERLINE && n_synthetic > 0)`, matching Block B's own guard so
the free is only attempted when the allocation actually happened), alongside the existing
`free(neighbors); free(class_idx[c]);` line.

Finally, update both existing call sites inside `mode_train()` to match the new signature,
inserting `SMOTE_STANDARD, NULL` as the two new arguments (before the three output-pointer
arguments) — this is a behavior-PRESERVING change for this plan (mode threading through
`mode_train()`'s own parameter list happens in plan 01-02; this task only makes the codebase
compile against the new signature while keeping today's behavior byte-for-byte identical,
since `SMOTE_STANDARD` was always the implicit behavior before this phase):
- Master call becomes: `smote_oversample(tr_x_v, tr_y_bin, n_train_aug, nf_vowel, 2, SMOTE_STANDARD, NULL, &os_m_x, &os_m_y, &os_n_m);`
- Expert call becomes: `smote_oversample(ex_tr_x, ex_tr_y, n_ex_tr, nf_vowel, 4, SMOTE_STANDARD, NULL, &os_e_x, &os_e_y, &os_n_e);`
  </action>
  <acceptance_criteria>
    - `grep -c "SmoteMode smote_mode, SmoteBorderlineCounts \*counts" src/main.c` outputs `1`
    - `grep -c "find_knn_global(x_in, class_idx\[c\]\[i\], n_in, nf, BORDERLINE_M_NEIGHBORS" src/main.c` outputs `1`
    - `grep -c "classify_borderline(m, kk)" src/main.c` outputs `1`
    - `grep -c "log_warn.*sem amostras borderline" src/main.c` outputs `1`
    - `grep -c "log_warn.*sintese pulada" src/main.c` outputs `1` (the n_class<=1 fallback message — confirms the SKIP path's warning text; there is no "log and continue producing the duplicate" message anywhere in the file)
    - `grep -c "log_warn.*sera duplicata\|sintetico sera duplicata" src/main.c` outputs `0` (the old log-and-duplicate message text must NOT be present — it was superseded by the skip fix)
    - `grep -A3 "if (n_class <= 1)" src/main.c | grep -c "n_synthetic = 0;"` outputs `1` (structural proof the n_class<=1 branch zeroes n_synthetic BEFORE the synthesis loop runs — this is what guarantees zero synthetic rows for that class, not merely a warning log)
    - `grep -c "\*n_out = out_idx;" src/main.c` outputs `1` (n_out is corrected to reflect the actual rows written, not the pre-allocated max_count * num_classes capacity — required so a skipped class's missing rows are never read as uninitialized memory downstream)
    - `grep -c "find_knn(x_in, base_idx, class_idx\[c\], n_class, nf, knn, neighbors)" src/main.c` outputs `1` (interpolation call site unchanged — never references class_idx_borderline)
    - `grep -c "smote_oversample(tr_x_v, tr_y_bin, n_train_aug, nf_vowel, 2, SMOTE_STANDARD, NULL" src/main.c` outputs `1`
    - `grep -c "smote_oversample(ex_tr_x, ex_tr_y, n_ex_tr, nf_vowel, 4, SMOTE_STANDARD, NULL" src/main.c` outputs `1`
    - `make 2>&1 | tee /tmp/build.log; grep -c "error:" /tmp/build.log` outputs `0`
    - `grep -c "warning:" /tmp/build.log` outputs at most `2` (the two pre-existing warnings documented in Phase 0's 00-02-SUMMARY.md: unused `external_dir` parameter in `mode_validate_external`, `fgets` ignored return value in `features_load_csv` — no new warnings introduced)
  </acceptance_criteria>
  <verify>
    <automated>make 2>&1 | tee /tmp/build.log; test "$(grep -c 'error:' /tmp/build.log)" = "0" && test "$(grep -c 'warning:' /tmp/build.log)" -le 2</automated>
  </verify>
  <done>smote_oversample() correctly implements both SMOTE_STANDARD (byte-for-byte identical to pre-phase behavior) and SMOTE_BORDERLINE (safe/borderline/noise classification via find_knn_global()/classify_borderline(), synthesis pool restricted to the borderline subset with a logged empty-pool fallback, interpolation always same-class via unchanged find_knn()); the pre-existing n_class<=1 case now SKIPS synthesis entirely (n_synthetic forced to 0, zero synthetic rows written, *n_out corrected to the actual out_idx row count) instead of producing a logged duplicate; both call sites compile against the new signature with explicit SMOTE_STANDARD, NULL arguments; SMOTE-01/02/03 satisfied at the code level, including SMOTE-03's literal "no degenerate synthetic samples" wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CLI argv -> mode dispatch | `base_dir`/mode strings are local, single-researcher-supplied CLI arguments — no network/remote input (per RESEARCH.md Security Domain: N/A, offline academic tool) |
| In-memory feature matrix -> smote_oversample() | Untrusted only in the sense of malformed/degenerate class distributions (e.g. n_class<=1, n_in very small) — not an adversarial input surface, but a correctness/robustness boundary this phase directly touches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | `find_knn_global()` / `smote_oversample()` new borderline pool loop | mitigate | Bound all new loops to `n_class`/`n_in` (already-known, finite, in-memory sizes); `safe_malloc` aborts cleanly on allocation failure rather than continuing with a corrupt pointer — no new unbounded loop introduced (no relaxation/retry loop in this phase, unlike Gap 1's paraconsistent threshold relaxation) |
| T-01-02 | Tampering | Cross-class label noise via accidental reuse of `find_knn_global()`'s neighbor list for interpolation | mitigate | SMOTE-02 is enforced structurally: the interpolation call site (`find_knn(x_in, base_idx, class_idx[c], n_class, ...)`) is left textually unchanged and is verified by an exact-string grep in this plan's own acceptance criteria — any future edit that substitutes `class_idx_borderline` there would fail this plan's own regression check if re-run |
| T-01-03 | Information Disclosure | N/A | accept | No new I/O, no new external data exposure — this phase is pure in-process numeric code, per RESEARCH.md's Security Domain audit (offline, single-user, no network exposure) |
| T-01-04 | Tampering (silent data corruption via stale row count) | `smote_oversample()`'s `*n_out` vs. actual rows written when a class's synthesis is skipped | mitigate | `*n_out = out_idx;` is set at the function's end (verified by this plan's own acceptance criteria grep), correcting the pre-allocated `max_count * num_classes` upper bound to the true number of written rows — prevents downstream `mlp_train()` calls from reading uninitialized `safe_malloc` memory for skipped classes' missing rows |
| T-01-SC | Tampering (supply chain) | N/A | accept | No package-manager installs in this phase — pure C99, zero external dependencies (RESEARCH.md "Package Legitimacy Audit: N/A") |

</threat_model>

<verification>
Run `make` (zero errors, at most the 2 pre-existing warnings). Run the grep-based acceptance criteria listed in each task. Full runtime proof of correctness (an actual training run exercising both SMOTE modes, including a real observation of how often the n_class<=1 skip path fires) is deliberately deferred to plan 01-03's dedicated A/B execution — this plan's scope is the algorithm's correctness by construction and clean compilation, not a full pipeline run.
</verification>

<success_criteria>
- `src/main.c` compiles cleanly with the new `SmoteMode`-aware `smote_oversample()`.
- `find_knn_global()` and `classify_borderline()` exist, are RNG-free, and use the untruncated `2*m >= k` boundary with `m == k` checked first.
- Both existing call sites explicitly pass `SMOTE_STANDARD, NULL`, preserving today's behavior exactly.
- SMOTE-03's two fallback cases (`n_class <= 1`, empty borderline pool) both log an explicit warning with no crash and no unbounded loop; the `n_class <= 1` case SKIPS synthesis entirely (zero synthetic rows for that class, `*n_out` corrected to the actual row count written) — no degenerate duplicate synthetic sample is ever produced, satisfying SMOTE-03's literal wording per the user's explicit decision.
</success_criteria>

<output>
Create `.planning/phases/01-gap-2-borderline-smote/01-01-SUMMARY.md` when done
</output>
