---
phase: 00-statistical-infrastructure-rng-reproducibility-prerequisite
plan: 03
type: execute
wave: 3
depends_on: ["00-01", "00-02"]
files_modified: [results/train_log_v29_baseline_reconfirmed.txt, results/metrics_global.csv, results/bootstrap_ci.csv, results/mcnemar_vs_baselines.csv]
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "A freshly executed, non-empty training log exists in the repo backing the SPEC/ROADMAP-cited baseline numbers (Macro F1 0.4423 / Accuracy 69.4%) — the previously SPEC-cited log (results/train_log_v29_late_fusion.txt) is empty (0 bytes) and cannot serve this purpose"
    - "The fresh run's Macro F1/Accuracy is either confirmed to match the reference within statistical noise, or an explicit, evidence-based divergence hypothesis is recorded — never a silent, unexplained gap"
  artifacts:
    - path: results/train_log_v29_baseline_reconfirmed.txt
      provides: "full stdout/stderr transcript of a fresh full-pipeline run (built on top of plans 00-01 and 00-02's fixes), ending with an explicit MATCHES/DIVERGES verdict against the SPEC-cited baseline"
  key_links:
    - from: "make full / build/vocal_detect full"
      to: "results/train_log_v29_baseline_reconfirmed.txt"
      via: "shell redirection during Task 1"
      pattern: "train_log_v29_baseline_reconfirmed"
    - from: "results/train_log_v29_baseline_reconfirmed.txt"
      to: "results/metrics_global.csv and results/bootstrap_ci.csv"
      via: "cross-referenced Macro F1/Accuracy/CI values in the Task 2 comparison verdict"
      pattern: "MATCHES reference|DIVERGES from reference"
---

<objective>
Produce the real, in-repo backing artifact for the SPEC.md/ROADMAP.md-cited v29 baseline (Macro F1 0.4423, Accuracy 69.4%) that every subsequent Phase 1-3 gap's A/B comparison will cite as "before." The log SPEC.md originally pointed to (results/train_log_v29_late_fusion.txt) is confirmed empty (0 bytes) -- this plan closes that gap with a freshly executed run captured end-to-end, run on top of this phase's own two prerequisite fixes (plan 00-01's RNG-race fix, plan 00-02's bootstrap-CI/McNemar wiring), so the reconfirmed baseline log is itself already statistically annotated and reproducibility-safe -- exactly what Phases 1-3 will need to diff against.

This plan depends on both 00-01 and 00-02: running before the RNG fix would produce a log with the same non-reproducibility problem being fixed; running before the stats wiring would miss the bootstrap CI/McNemar sections this log should already demonstrate as a smoke test of plan 00-02's work.

Purpose: Close STATE.md's standing blocker ("SPEC.md-cited baseline... has no backing log in-repo yet") and give Phases 1-3 a citable, reproducible "before" artifact.
Output: results/train_log_v29_baseline_reconfirmed.txt with an explicit match/divergence verdict against the cited reference numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
No code is modified by this plan. These are the exact commands and file formats the executor
needs to run the pipeline and interpret its output.

Makefile "full" target (repo root) runs: ./build/vocal_detect full
Equivalent direct invocation used by this plan, to control log-capture redirection precisely:
./build/vocal_detect full .

Current results/metrics_global.csv format (src/metrics.c metrics_export_csv), columns are
class,precision,recall,f1,support:

  class,precision,recall,f1,support
  Normal,0.814249,0.931587,0.868975,687
  Laringite,0.505882,0.307143,0.382222,140
  Disfonia Psicogenica,0.297619,0.274725,0.285714,91
  Disfonia Funcional,0.381818,0.187500,0.251497,112
  Edema de Reinke,0.375000,0.485294,0.423077,68
  macro_avg,,,0.442297,1098
  weighted_avg,,,0.667972,1098
  accuracy,,,0.693989,1098

The "macro_avg" row's 4th column (f1) is Macro F1 (0.442297, matching SPEC's cited "0,4423"). The
"accuracy" row's 4th column (f1) holds the accuracy value (0.693989, matching SPEC's cited
"69,4%"). This is the CURRENT file, from a prior undocumented run, before this plan's fresh run
overwrites it -- it already closely matches the SPEC-cited baseline, which is itself evidence the
pipeline is close to reproducible even pre-fix; this plan's job is to produce a properly logged,
dated, freshly-executed run and confirm (or document divergence from) the same numbers.

results/bootstrap_ci.csv and results/mcnemar_vs_baselines.csv (produced by plan 00-02's wiring)
will be freshly regenerated by this run and should be referenced in the Task 2 comparison verdict,
to judge whether any observed Macro F1/Accuracy difference from the reference falls inside the 95
percent bootstrap CI (the CI_MACRO_F1 / CI_ACCURACY rows of bootstrap_ci.csv).

Reference baseline values (ROADMAP.md Phase 0 success criterion 3, STATE.md, SPEC.md): Macro F1
0.4423 (written as "0,4423" in the project's Brazilian-Portuguese decimal notation), Accuracy
69.4 percent (0.694).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run a fresh full training pipeline and capture the baseline log</name>
  <files>results/train_log_v29_baseline_reconfirmed.txt</files>
  <read_first>
    Makefile (full target)
    CLAUDE.md, section "Build and Run Commands" for the expected ~60-90 min training time, and "Current Best Results" for the SPEC-cited baseline numbers
  </read_first>
  <action>
From the repo root, run the trained binary directly, redirecting both stdout and stderr into the target log file via tee so the file is populated live: ./build/vocal_detect full . 2>&1 | tee results/train_log_v29_baseline_reconfirmed.txt -- this exercises plan 00-01's RNG fix and plan 00-02's bootstrap-CI/McNemar wiring, both already merged into src/main.c by this wave. Do NOT run make clean first: results/features.csv is a valid, extraction-deterministic cache (CONCERNS.md confirms pure feature extraction never touches the RNG and is already reproducible), and reusing it does not affect this run's validity -- training-stage reproducibility, the thing this phase actually fixes, is independent of whether the feature cache was just re-extracted or reused. This run is expected to take on the order of 60-90 minutes per CLAUDE.md's documented training time for the current 251-feature hierarchical-late-fusion pipeline; if the execution environment enforces a hard command timeout shorter than that, run it in the background and poll for completion rather than re-invoking it. After the run completes, confirm results/metrics_global.csv, results/bootstrap_ci.csv, and results/mcnemar_vs_baselines.csv were all freshly regenerated (their modification times must be after this task's run started -- check with `ls -la` or `stat`).
  </action>
  <acceptance_criteria>
    - results/train_log_v29_baseline_reconfirmed.txt exists, is non-empty, and contains the literal substring "Macro F1"
    - results/train_log_v29_baseline_reconfirmed.txt contains exactly 5 fold-result banners (grep -c "FOLD .*HIERARCHICAL LATE FUSION" results/train_log_v29_baseline_reconfirmed.txt equals 5)
    - results/metrics_global.csv, results/bootstrap_ci.csv, and results/mcnemar_vs_baselines.csv all have a modification time newer than the timestamp recorded at the start of this task
  </acceptance_criteria>
  <verify>
    <automated>REPO="/home/ronaldo/Área de trabalho/modelo_ml_mlp_v3"; cd "$REPO" && test -s results/train_log_v29_baseline_reconfirmed.txt && grep -q "Macro F1" results/train_log_v29_baseline_reconfirmed.txt && N=$(grep -c "FOLD .*HIERARCHICAL LATE FUSION" results/train_log_v29_baseline_reconfirmed.txt); test "$N" = "5"</automated>
  </verify>
  <done>A freshly executed, non-empty training log exists at results/train_log_v29_baseline_reconfirmed.txt for the current v29 HEAD code, including the Phase 0 RNG fix and statistical-infrastructure wiring.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Compare the fresh run against the SPEC-cited baseline and document match or divergence</name>
  <files>results/train_log_v29_baseline_reconfirmed.txt</files>
  <read_first>
    results/train_log_v29_baseline_reconfirmed.txt (Task 1's output)
    results/metrics_global.csv (freshly regenerated by Task 1)
    results/bootstrap_ci.csv (freshly regenerated by Task 1, via plan 00-02's wiring)
    CLAUDE.md, "Current Best Results" section, for the v29 baseline reference values
  </read_first>
  <action>
Extract the freshly-run Macro F1 and Accuracy from results/metrics_global.csv: the "macro_avg" row's 4th (f1) column is Macro F1, and the "accuracy" row's 4th (f1) column is the accuracy value. Compare both against the SPEC/ROADMAP-cited reference values (Macro F1 0.4423, Accuracy 0.694). Then read results/bootstrap_ci.csv's CI_MACRO_F1 and CI_ACCURACY rows (their ci_lower/ci_upper bounds). Append a short, explicit comparison section to the END of results/train_log_v29_baseline_reconfirmed.txt (append only -- do not overwrite the training transcript already captured by Task 1) containing: the freshly measured Macro F1 and Accuracy, the reference values, the absolute difference for each, and exactly one of these two literal verdict phrases: "MATCHES reference" if both reference values fall within their respective bootstrap 95 percent CI bounds from this run, or "DIVERGES from reference" otherwise.

If "DIVERGES from reference" applies, also append two concrete, code-grounded hypotheses for the cause (both must be named -- this run sits downstream of two separate RNG-sequence-altering changes made earlier in this same phase, and either or both may contribute):

1. **RNG-race fix (plan 00-01):** Removing the `#pragma omp parallel for` from `precalculate_augmentations()` necessarily changed the exact sequence of `rng_*` draws consumed by SMOTE, dropout, and weight initialization relative to whatever single historical run produced the previously-recorded 0.4423/0.694 numbers -- and that OLD number was itself never guaranteed reproducible run-to-run before this phase's fix (PITFALLS.md Pitfall 13).

2. **Baseline RNG consumption (plan 00-02):** `lr_train()`'s internal `rng_normal()`/`rng_shuffle_int()`/`rng_normal()` calls (for its noise-injection step) now run once per fold, BEFORE the per-vowel SMOTE+MLP training loop, because plan 00-02 placed the MajorityClass/kNN/LogisticRegression baseline computation ahead of the MLP training block in `mode_train()`. This consumes additional draws from the same global `rng_state` earlier in each fold's sequence than in any pre-00-02 run, independently shifting what `smote_oversample()`/`mlp_init_dynamic()`/dropout subsequently draw -- a second, independent contributor separate from cause 1.

State explicitly that a small divergence is an anticipated, not alarming, consequence of closing INFRA-02 and INFRA-01 together, not evidence of a new bug -- since both causes are deterministic, documented, and reproducible themselves (per plan 00-01's own byte-identical `verify-rng` proof), just different from the undocumented single run that originally produced 0.4423/0.694.
  </action>
  <acceptance_criteria>
    - results/train_log_v29_baseline_reconfirmed.txt contains one of the literal substrings "MATCHES reference" or "DIVERGES from reference"
    - if "DIVERGES from reference" is present, the file also contains the substring "RNG" somewhere in the same appended section, naming the hypothesis
    - if "DIVERGES from reference" is present, the appended section names BOTH contributing causes: the substring "00-01" (or "RNG-race" / "precalculate_augmentations") for the first cause, AND the substring "00-02" (or "lr_train" / "baseline") for the second cause -- a single-cause explanation is incomplete per this plan's revised objective
    - the appended section explicitly states both the fresh Macro F1/Accuracy values and the 0.4423/0.694 reference values side by side
  </acceptance_criteria>
  <verify>
    <automated>REPO="/home/ronaldo/Área de trabalho/modelo_ml_mlp_v3"; F="$REPO/results/train_log_v29_baseline_reconfirmed.txt"; grep -Eq "MATCHES reference|DIVERGES from reference" "$F" && { ! grep -q "DIVERGES from reference" "$F" || (grep -qiE "00-01|RNG-race|precalculate_augmentations" "$F" && grep -qiE "00-02|lr_train|baseline" "$F"); }</automated>
  </verify>
  <done>results/train_log_v29_baseline_reconfirmed.txt ends with an explicit, evidence-based match/divergence verdict against the SPEC-cited baseline (Macro F1 0.4423 / Accuracy 69.4%), and if diverging, names both RNG-sequence-altering causes (plan 00-01's race fix AND plan 00-02's baseline RNG consumption), satisfying ROADMAP Phase 0 success criterion 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same local, single-user, offline CLI pipeline as plans 00-01/00-02 -- no new trust boundary. This
plan modifies no source code; it only executes the already-reviewed binary and writes results/
artifacts.

| Boundary | Description |
|----------|--------------|
| Long-running background process output -> log file | Purely local file I/O via shell redirection; no new input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-00-07 | Repudiation (an academic claim of "reproduced baseline" without honest divergence disclosure) | results/train_log_v29_baseline_reconfirmed.txt's comparison verdict | mitigate | Task 2 requires an explicit, literal MATCHES/DIVERGES verdict backed by the actual bootstrap CI bounds from this exact run, plus BOTH concrete code-grounded hypotheses (00-01's RNG-race fix AND 00-02's baseline RNG consumption) if divergence occurs -- never a silent, vague, or single-cause statement. |
| T-00-08 | Denial of Service (malformed WAV causing a crash mid-run, wasting the 60-90 minute run) | src/wav_io.c wav_read() (pre-existing, out of Phase 0 scope) | accept | Dataset is confirmed clean (CONCERNS.md: "0 erros" across 1098 patients); not touched or worsened by this plan; a future tech-debt phase item, not reintroduced here. |
| T-00-SC | Supply chain (package manager installs) | N/A | accept | This plan runs an already-built binary and reads/writes local text/CSV files only; no package installs anywhere in this plan. |
</threat_model>

<verification>
1. results/train_log_v29_baseline_reconfirmed.txt exists, is non-empty, contains "Macro F1", and shows exactly 5 fold banners.
2. results/metrics_global.csv, results/bootstrap_ci.csv, and results/mcnemar_vs_baselines.csv are all freshly regenerated (mtimes after the run started), confirming plans 00-01 and 00-02's wiring survived an actual end-to-end run.
3. results/train_log_v29_baseline_reconfirmed.txt ends with an explicit MATCHES/DIVERGES verdict, with both contributing hypotheses (00-01 and 00-02) named if diverging.
</verification>

<success_criteria>
- results/train_log_v29_baseline_reconfirmed.txt exists with a freshly executed run's Macro F1/Accuracy (INFRA-03 satisfied).
- The fresh run's numbers either match the SPEC-cited baseline (0.4423 / 69.4%) within the run's own bootstrap CI, or the divergence is explicitly documented with both concrete, code-grounded hypotheses (plan 00-01's RNG-race fix and plan 00-02's baseline RNG consumption).
- This run doubles as an integration smoke test proving plans 00-01 (RNG fix) and 00-02 (statistical wiring) work correctly together end-to-end, not just in isolated static checks.
</success_criteria>

<output>
Create `.planning/phases/00-statistical-infrastructure-rng-reproducibility-prerequisite/00-03-SUMMARY.md` when done
</output>
