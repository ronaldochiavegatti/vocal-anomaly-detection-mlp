---
phase: 00-statistical-infrastructure-rng-reproducibility-prerequisite
plan: 03
subsystem: infra
tags: [reproducibility, baseline, bootstrap-ci, mcnemar, documentation]

# Dependency graph
requires:
  - "00-01: RNG race fix (precalculate_augmentations is deterministic under RANDOM_SEED=42)"
  - "00-02: Bootstrap CI + 3-way McNemar wired into mode_train()"
provides:
  - "results/train_log_v29_baseline_reconfirmed.txt: freshly executed, non-empty, 467-line (+37-line verdict appendix) full-pipeline transcript backing the SPEC.md/ROADMAP.md-cited baseline (Macro F1 0.4423 / Accuracy 69.4%)"
  - "An explicit, evidence-based MATCHES reference verdict, cross-referencing this run's own 95% bootstrap CI bounds"
  - "results/metrics_global.csv, results/bootstrap_ci.csv, results/mcnemar_vs_baselines.csv freshly regenerated by an actual end-to-end run (first runtime proof of plan 00-02's wiring, not just a static code check)"
affects: [01-gap2-borderline-smote, 02-gap3-shallow-vs-deep, 03-gap1-paraconsistent-selection]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Background-run-and-poll pattern for multi-hour CLI pipelines: nohup-detach the process, then block on a foreground wait-loop (pgrep-based) so a hard per-call timeout auto-backgrounds the wait itself and notifies the agent on completion, instead of firing-and-forgetting a background job and assuming it will report back unprompted"
    - "Split-file-then-commit pattern for atomic per-task commits against a single shared output file: temporarily truncate the file to its pre-second-task-edit content, commit as task 1, restore the full content, commit the diff as task 2"

key-files:
  created: []
  modified:
    - results/train_log_v29_baseline_reconfirmed.txt
    - results/metrics_global.csv
    - results/bootstrap_ci.csv
    - results/mcnemar_vs_baselines.csv

key-decisions:
  - "Did not run `make clean` before the fresh run (results/features.csv was actually absent from this working tree at plan start, so extraction ran fresh anyway, ~unmeasured but well under a minute given prior 39-42s benchmarks) -- consistent with the plan's instruction that feature-extraction-cache reuse/absence does not affect training-stage reproducibility validity"
  - "Verdict is MATCHES reference: fresh Macro F1=0.451440 and Accuracy=0.697632 both fall inside this run's own 95% bootstrap CI for the reference values (0.4423 within [0.415139, 0.489630]; 0.6940 within [0.671220, 0.724954]) -- no divergence-hypothesis section was needed, per the plan's conditional acceptance criteria"

patterns-established:
  - "Any future full-pipeline reconfirmation run should be captured with `tee` (not just `>`) so live progress is visible during the required background-poll wait, in addition to the final durable log artifact"

requirements-completed: [INFRA-03]

# Metrics
duration: 43min
completed: 2026-07-27
---

# Phase 0 Plan 3: Baseline Reconfirmation (Fresh Full-Pipeline Run) Summary

**Executed a fresh, end-to-end `./build/vocal_detect full .` run on top of Phase 0's RNG-race fix (00-01) and statistical-infrastructure wiring (00-02), producing the first in-repo, non-empty, dated training log backing the SPEC.md/ROADMAP.md-cited v29 baseline (Macro F1 0.4423 / Accuracy 69.4%) -- and confirmed a MATCHES reference verdict via bootstrap CI containment.**

## Performance

- **Duration:** 43 min (background pipeline run: 16:30:03-17:01:40 local, ~31.6 min wall time for the actual 5-fold hierarchical late fusion training + feature extraction from an absent cache; remainder was verification, verdict authorship, and atomic commits)
- **Completed:** 2026-07-27
- **Tasks:** 2
- **Files modified:** 4 (`results/train_log_v29_baseline_reconfirmed.txt` created, `results/metrics_global.csv`/`results/bootstrap_ci.csv`/`results/mcnemar_vs_baselines.csv` regenerated)

## Accomplishments

- **Task 1:** Ran `./build/vocal_detect full . 2>&1 | tee results/train_log_v29_baseline_reconfirmed.txt` as a detached background process (`nohup`), polled to completion via a foreground `pgrep`-based wait-loop rather than assuming an unprompted notification, per this plan's explicit process-handling instructions. `results/features.csv` was actually absent from the working tree at plan start (not merely stale), so this run also exercised a full feature re-extraction alongside training. Produced a 467-line log containing "Macro F1" and exactly 5 `FOLD .*HIERARCHICAL LATE FUSION` banners; confirmed `results/metrics_global.csv`, `results/bootstrap_ci.csv`, and `results/mcnemar_vs_baselines.csv` were all freshly regenerated (mtime 2026-07-27 17:01:40, after the run started).
- **Task 2:** Extracted fresh Macro F1 (0.451440) and Accuracy (0.697632) from `results/metrics_global.csv`'s `macro_avg`/`accuracy` rows, and the 95% bootstrap CI bounds for both from `results/bootstrap_ci.csv` (CI_MACRO_F1 [0.415139, 0.489630], CI_ACCURACY [0.671220, 0.724954]). Both SPEC-cited reference values (0.4423 / 0.6940) fall inside their respective CI bounds, so appended a 37-line Portuguese-language comparison section to the end of the log with the literal verdict `MATCHES reference`, a side-by-side value table, and an explanation of why the small observed deltas (+0.009140 Macro F1, +0.003632 Accuracy) are expected bootstrap noise, not a regression.

## Task Commits

Each task was committed atomically:

1. **Task 1: Run a fresh full training pipeline and capture the baseline log** - `fdb51ce` (feat)
2. **Task 2: Compare the fresh run against the SPEC-cited baseline and document match/divergence** - `1b9a993` (docs)

**Plan metadata:** (this commit) `docs(00-03): complete baseline reconfirmation plan`

## Files Created/Modified

- `results/train_log_v29_baseline_reconfirmed.txt` - New: full stdout/stderr transcript of the fresh `make full`-equivalent run (per-fold hierarchical late-fusion training, confusion matrices, per-class P/R/F1, aggregate Macro F1/Accuracy, bootstrap CI, 3-way McNemar), plus an appended MATCHES/DIVERGES verdict section
- `results/metrics_global.csv` - Regenerated: fresh point-estimate per-class/macro/weighted/accuracy metrics (Macro F1=0.451440, Accuracy=0.697632; previous file's committed values were 0.442297/0.693989, from an undocumented prior run)
- `results/bootstrap_ci.csv` - Regenerated: 8-line (header + 7 metrics) 95% bootstrap CI file, first real runtime output of plan 00-02's wiring
- `results/mcnemar_vs_baselines.csv` - Regenerated: 4-line (header + 3 baselines) 3-way McNemar significance results, first real runtime output of plan 00-02's wiring

## Decisions Made

- Ran the pipeline without `make clean` and without pre-seeding `results/features.csv` from a prior cache -- the cache was genuinely absent at plan start (not stale), so this run extracted features from scratch as an incidental side effect, consistent with the plan's guidance that cache state does not affect training-stage reproducibility validity.
- Judged MATCHES reference (not DIVERGES) because both reference values are contained within this run's own bootstrap CI bounds, per the plan's explicit acceptance criterion; the two RNG-cause hypotheses (00-01 race fix, 00-02 baseline RNG consumption) were therefore not required in the appended section, though both are named in general terms as expected sources of run-to-run variability.
- Split the single shared output file into two separate git commits (raw pipeline transcript for Task 1, appended verdict for Task 2) by temporarily truncating the file to its pre-Task-2 state, committing, then restoring the full content and committing the diff -- preserving one-commit-per-task atomicity despite both tasks writing to the same file.

## Deviations from Plan

### Auto-fixed Issues

None - plan executed exactly as written. The only operational adaptation was the background-run/poll process-handling mechanics explicitly mandated by the plan's own `<critical_process_note>`, not a deviation from the plan's substantive content.

## Issues Encountered

- The pipeline's actual wall-clock time (~31.6 min) was substantially shorter than CLAUDE.md's documented 60-90 min estimate for the 237-feature configuration, likely due to the 12-core machine's fast feature extraction and/or the current feature-selection stage reducing the effective per-fold training dimensionality. Not a concern for this plan's acceptance criteria (log content and artifact freshness), noted here only for future duration-estimate calibration.
- A first attempt to write a PID file for the backgrounded process silently failed (scratchpad subdirectory did not yet exist); recovered by confirming the process was running via `ps aux` and using a `pgrep`-based wait-loop instead of relying on the PID file, with no impact on run correctness.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- INFRA-03 is satisfied: a freshly executed, in-repo, dated training log now backs the SPEC.md/ROADMAP.md-cited v29 baseline, closing STATE.md's standing blocker.
- Phase 0 (statistical-infrastructure-rng-reproducibility-prerequisite) is now complete: all 3 plans done (00-01 RNG race fix, 00-02 bootstrap CI/McNemar wiring, 00-03 baseline reconfirmation).
- Phase 1 (Gap 2: Borderline-SMOTE) can now cite `results/train_log_v29_baseline_reconfirmed.txt` as its reproducible "before" artifact for its own A/B comparison, with statistical infrastructure (bootstrap CI, McNemar) already proven to work end-to-end.
- No blockers identified.

---
*Phase: 00-statistical-infrastructure-rng-reproducibility-prerequisite*
*Completed: 2026-07-27*

## Self-Check: PASSED

- FOUND: results/train_log_v29_baseline_reconfirmed.txt
- FOUND: results/metrics_global.csv
- FOUND: results/bootstrap_ci.csv
- FOUND: results/mcnemar_vs_baselines.csv
- FOUND: commit fdb51ce
- FOUND: commit 1b9a993
