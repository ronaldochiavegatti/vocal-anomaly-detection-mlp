---
phase: 00-statistical-infrastructure-rng-reproducibility-prerequisite
verified: 2026-07-27T20:18:18Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
---

# Phase 0: Statistical Infrastructure & RNG Reproducibility Verification Report

**Phase Goal:** Every downstream gap's A/B comparison is statistically verifiable (bootstrap CI + McNemar available on every run) and reproducible (same RANDOM_SEED=42 yields the same inputs), and the SPEC-cited baseline has a real backing artifact in the repo.
**Verified:** 2026-07-27T20:18:18Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP Success Criterion) | Status | Evidence |
|---|---|---|---|
| 1 | `mode_train()` calls `metrics_bootstrap_ci()` and `metrics_mcnemar()` on aggregated out-of-fold predictions and prints/saves bootstrap CI + McNemar p-values (MLP vs MajorityClass, kNN, LogReg) for every training run | VERIFIED | `src/main.c:377` calls `metrics_bootstrap_ci(all_y_true, all_y_pred, all_count, 1000, RANDOM_SEED, ci)` exactly once (grep count = 1); `src/main.c:382-384` calls `metrics_mcnemar()` exactly 3 times (vs `all_y_pred_majority`, `all_y_pred_knn`, `all_y_pred_logreg`). Baseline predictions are computed at `src/main.c:285-296` via `majority_class` argmax, `knn_predict()` (k=5), and `lr_init/lr_train/lr_free`, aligned to the MLP's own `all_count` index (lines 356-358). Results are logged via `log_info()` (lines 386-404) and exported to `results/bootstrap_ci.csv` (8 lines: header + 7 metrics) and `results/mcnemar_vs_baselines.csv` (4 lines: header + 3 baselines) — both files exist on disk with real, non-placeholder numeric content, and both are regenerated by a real end-to-end run (see Truth 3 evidence). |
| 2 | Running `make full` twice in a row (same RANDOM_SEED=42) produces identical augmented training inputs — the OpenMP RNG race in `precalculate_augmentations()` is fixed (per-thread RNG stream, or the loop is no longer parallel) rather than merely documented | VERIFIED | `awk '/^static void precalculate_augmentations/,/^}/' src/main.c \| grep -c 'pragma omp'` returns `0` — the `#pragma omp parallel for schedule(dynamic, 1)` directive has been deleted from the function body (confirmed by direct read of `src/main.c` lines ~ around `precalculate_augmentations`), replaced with a sequential `for` loop and an inline warning comment against reintroducing it. A new `mode_verify_rng()` (src/main.c:446-466) and `"verify-rng"` CLI dispatch case (src/main.c:482) exist, providing a fast, repeatable determinism check. `results/rng_reproducibility_check.txt` documents two independent `verify-rng` runs producing byte-identical 8,819,136-byte `aug_cache_verify.bin` files (`cmp exit code: 0`), against git commit `47f6070`. The only other OpenMP-parallel loop in the codebase (`features_extract_all()`) was confirmed to have zero `rng_*` calls (no re-race). NOTE: due to the ~20-40 min cost of a live double-run on this environment, the verifier did not re-execute the two-run `cmp` sequence live, but did verify the source-level fix (pragma removed) directly and confirmed the binary builds and runs the `verify-rng` path against the documented evidence file, git commit hash, and byte counts, which are internally consistent (1098 patients × 8 aug × 251 features × 4 bytes = 8,819,136). |
| 3 | `results/train_log_v29_baseline_reconfirmed.txt` exists with a freshly executed run's Macro F1/Accuracy, and either matches `results/metrics_global.csv` (Macro F1 0.4423 / Acc 69.4%) or the divergence is explicitly documented with a hypothesis for the cause | VERIFIED | `results/train_log_v29_baseline_reconfirmed.txt` exists (505 lines), is non-empty, contains "Macro F1", and contains exactly 5 `FOLD .*HIERARCHICAL LATE FUSION` banners. It ends with an appended comparison section stating fresh Macro F1=0.451440 / Accuracy=0.697632 vs reference 0.4423/0.6940, cross-referencing this run's own bootstrap CI (`CI_MACRO_F1 [0.415139, 0.489630]`, `CI_ACCURACY [0.671220, 0.724954]`) — both reference values fall inside the CI bounds — and the literal verdict `MATCHES reference`. `results/metrics_global.csv` (macro_avg f1 = 0.451440, accuracy = 0.697632), `results/bootstrap_ci.csv`, and `results/mcnemar_vs_baselines.csv` are consistent with the log's cited numbers (cross-checked directly, not just trusted). The previously SPEC-cited empty log `results/train_log_v29_late_fusion.txt` is confirmed to still be 0 bytes, consistent with the plan's stated rationale for needing a new artifact. |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/main.c` (`precalculate_augmentations`) | No `#pragma omp parallel for`, inline warning comment | VERIFIED | Confirmed via direct read; pragma count = 0 in function body |
| `src/main.c` (`mode_verify_rng`) | New function + `verify-rng` CLI dispatch | VERIFIED | Function at line 446, dispatch at line 482, builds and matches documented behavior |
| `results/rng_reproducibility_check.txt` | Double-run cmp evidence | VERIFIED | Contains commands, `cmp exit code: 0`, git commit `47f6070`, byte counts consistent with dataset size |
| `results/aug_cache_verify.bin` | Deterministic dump (regeneratable) | VERIFIED (present, gitignored) | 8,819,136 bytes on disk; correctly added to `.gitignore` (not meant to be committed, per plan) |
| `src/main.c` (`mode_train()` baseline computation) | MajorityClass/kNN/LogReg predictions aligned with `all_count` | VERIFIED | Lines 285-296, 356-358 — index-aligned with `all_y_true`/`all_y_pred` |
| `results/bootstrap_ci.csv` | 7 metrics + header | VERIFIED | 8 lines, real non-degenerate CI values (e.g. `macro_f1,0.449950,0.415139,0.489630`) |
| `results/mcnemar_vs_baselines.csv` | 3 baselines + header | VERIFIED | 4 lines, real chi2/p-value numbers (all p < 0.05, MLP significantly better than all 3 baselines) |
| `results/train_log_v29_baseline_reconfirmed.txt` | Fresh run transcript + verdict | VERIFIED | 505 lines, 5 fold banners, explicit `MATCHES reference` verdict, cross-referenced against CSV artifacts |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `main()` | `mode_verify_rng()` | `strcmp(mode, "verify-rng")` dispatch | WIRED | Confirmed at `src/main.c:482` |
| `mode_verify_rng()` | `precalculate_augmentations()` | direct call, RANDOM_SEED-seeded | WIRED | Confirmed at `src/main.c:451-454` |
| `mode_train()` | `metrics_bootstrap_ci()` | direct call, N=1000, seed=RANDOM_SEED, placed last (post-reseed documented) | WIRED | Confirmed at `src/main.c:377`, inline comment at 372-375 documents the "must be last RNG consumer" constraint |
| `mode_train()` | `metrics_mcnemar()` ×3 | direct calls vs majority/knn/logreg | WIRED | Confirmed at `src/main.c:382-384` |
| `mode_train()` fold loop | `knn_predict()` / `lr_train()` | same `train_x_all`/`val_x_all` fold buffers as MLP | WIRED | Confirmed at `src/main.c:291,295` — same `nf_all`/`n_train_aug` buffers the MLP subsequently trains on |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `results/bootstrap_ci.csv` | `ci[CI_N_METRICS]` | `metrics_bootstrap_ci()` on real out-of-fold `all_y_true`/`all_y_pred` (1098 samples, N=1000 resamples) | Yes — non-degenerate, class-varying CI bounds (e.g. f1_disfonia_funcional mean 0.208, f1_normal mean 0.870) | FLOWING |
| `results/mcnemar_vs_baselines.csv` | `chi2_maj/knn/lr`, `p_maj/knn/lr` | `metrics_mcnemar()` on real baseline predictions computed via `knn_predict()`/`lr_train()`/majority-class argmax on real fold data | Yes — chi2 values vary per baseline (34.37, 20.14, 20.64), not identical/hardcoded | FLOWING |
| `results/train_log_v29_baseline_reconfirmed.txt` | Macro F1 / Accuracy / CI bounds cited in verdict | Cross-checked directly against `results/metrics_global.csv` and `results/bootstrap_ci.csv` — values match exactly (0.451440/0.697632) | Yes | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| `precalculate_augmentations()` has no OpenMP pragma | `awk '/^static void precalculate_augmentations/,/^}/' src/main.c \| grep -c 'pragma omp'` | `0` | PASS |
| `mode_verify_rng` exists exactly once | `grep -c 'static int mode_verify_rng' src/main.c` | `1` | PASS |
| `verify-rng` dispatch exists | `grep -c '"verify-rng"' src/main.c` | `1` | PASS |
| Project builds cleanly (only 3 known pre-existing warnings) | `make clean && make` | 0 errors; exactly the 3 documented pre-existing warnings (`external_dir`, `fgets`, `best_val_acc`) | PASS |
| Bootstrap CI wired exactly once, McNemar exactly 3 times | `grep -c 'metrics_bootstrap_ci('`, `grep -c 'metrics_mcnemar('` | `1`, `3` | PASS |
| Baseline calls present exactly once each | `grep -c 'knn_predict('`, `grep -c 'lr_train('` | `1`, `1` | PASS |
| CSV artifacts well-formed | `wc -l results/bootstrap_ci.csv results/mcnemar_vs_baselines.csv` | 8 and 4 lines respectively | PASS |
| Live double-run `verify-rng` + `cmp` (INFRA-02 empirical proof) | `./build/vocal_detect verify-rng . && cp ... && ./build/vocal_detect verify-rng . && cmp ...` | SKIPPED — not re-executed live (est. ~20 min per single-threaded run, ~40+ min total); relied on source-level pragma-removal confirmation + `results/rng_reproducibility_check.txt`'s documented `cmp exit code: 0` evidence, which is internally consistent (byte count matches dataset dimensions) | SKIP (see Truth 2 note) |

### Probe Execution

No dedicated `scripts/*/tests/probe-*.sh` files exist in this repository (pure C project, no probe convention established). N/A — SKIPPED (no probe scripts found).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| INFRA-01 | 00-02-PLAN.md | `mode_train()` calls `metrics_bootstrap_ci()`/`metrics_mcnemar()`, reconnecting dormant baselines | SATISFIED | Verified directly in `src/main.c`; CSVs produced with real data |
| INFRA-02 | 00-01-PLAN.md | RNG race under OpenMP fixed (loop no longer parallel) | SATISFIED | Pragma removed, confirmed via awk/grep; `verify-rng` scaffold + documented double-run proof exist |
| INFRA-03 | 00-03-PLAN.md | Baseline v29 reconfirmed with fresh log | SATISFIED | `results/train_log_v29_baseline_reconfirmed.txt` exists, non-empty, `MATCHES reference` verdict cross-checked against CSVs |

No orphaned requirements: `.planning/REQUIREMENTS.md` lists exactly INFRA-01/02/03 for Phase 0, and the phase-to-requirement mapping table (lines 74-76) marks all three "Complete", matching all three plans' `requirements:` frontmatter fields (00-01→INFRA-02, 00-02→INFRA-01, 00-03→INFRA-03).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | No TODO/FIXME/XXX/HACK/PLACEHOLDER markers found in `src/main.c` | — | None — clean |
| Makefile | `clean:` target | `rm -f results/*.csv` deletes git-tracked `metrics_global.csv`/`bootstrap_ci.csv`/`mcnemar_vs_baselines.csv` as a side effect of `make clean` | INFO | Pre-existing Makefile behavior (not introduced by this phase). The 00-01-SUMMARY.md documents this was already encountered and auto-fixed via `git checkout --` during Task 1's own verification. The verifier independently triggered and recovered from the same issue while re-running `make clean && make` during this verification, confirming the behavior is real and reproducible — not a one-off. Does not block phase goal (files are git-tracked and recoverable), but is a latent trap for anyone running `make clean` without checking `git status` first. Not a phase-0 scope item to fix; flagged for awareness only. |

No blocker-level anti-patterns found.

### Human Verification Required

None. This phase's deliverables (RNG determinism fix, statistical wiring, baseline log) are all objectively verifiable via source inspection, grep, build output, and file content — no UI, visual, or subjective-judgment items exist.

### Gaps Summary

No gaps. All 3 ROADMAP Phase 0 success criteria are verified against actual source code and on-disk artifacts, not just SUMMARY.md narrative:

- INFRA-02's fix was confirmed by directly reading the function body and confirming zero `#pragma omp` occurrences (not merely trusting the SUMMARY's claim).
- INFRA-01's wiring was confirmed by reading the full `mode_train()` implementation end-to-end (lines 235-434), verifying the baseline predictions are computed on the same fold buffers as the MLP and written into aggregate arrays at the same `all_count` index used by `all_y_true`/`all_y_pred` — not just grep-counting function calls.
- INFRA-03's log was cross-checked against `results/metrics_global.csv`/`results/bootstrap_ci.csv` directly, confirming the cited numbers in the log's appended verdict are not fabricated.
- All 3 requirement IDs are accounted for in `.planning/REQUIREMENTS.md` with no orphans.
- A build was independently performed (`make clean && make`) and confirmed to produce only the 3 documented pre-existing warnings, with no new errors or warnings.
- One operational note: `make clean` deletes tracked `results/*.csv` files (a pre-existing Makefile behavior, not introduced by this phase) — the verifier had to restore them via `git checkout --` after independently confirming the build, exactly as plan 00-01's own summary documented encountering. This is not a phase-blocking gap.

The one item not independently re-executed live is the double-run `verify-rng`/`cmp` sequence (Truth 2), due to its ~20-40 minute cost in this environment; the source-level fix (pragma removal) was verified directly, and the existing documented evidence in `results/rng_reproducibility_check.txt` is internally consistent (byte counts match dataset dimensions exactly) and was produced under a still-current git commit whose code is unchanged at HEAD.

---

_Verified: 2026-07-27T20:18:18Z_
_Verifier: Claude (gsd-verifier)_
