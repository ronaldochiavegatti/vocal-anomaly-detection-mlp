---
phase: 02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: [CLAUDE.md]
autonomous: true
requirements: [ARCH-06]

must_haves:
  truths:
    - "CLAUDE.md accurately reflects the real, tested Gap 3 (shallow vs deep MLP) outcome, whichever architecture was adopted"
    - "CLAUDE.md's MLP Architecture section states the adopted architecture as production-relevant, with the Config C mislabeling correction (Plan 02-01) still intact and not reverted"
    - "The stale L2 lambda value in CLAUDE.md's Key Hyperparameters table is corrected to match the actual config.h constant this phase's regularization sweep was built on"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected Key Hyperparameters L2 lambda row + a new dated Gap 3 Outcome subsection with the 12-arm comparison table summary, 1-SE/McNemar decision, and file pointers"
      contains: "Gap 3 Outcome"
  key_links:
    - from: "CLAUDE.md Gap 3 Outcome subsection"
      to: "results/train_log_v33_gap3_arch_compare.txt"
      via: "file-path reference"
      pattern: "train_log_v33_gap3_arch_compare.txt"
---

<objective>
Document the real Gap 3 (shallow vs. deep MLP comparison) outcome in `CLAUDE.md` — the adopted
architecture and regularization setting, the 1-SE + McNemar decision, and the Pitfall 3
fixed-hyperparameter caveat — regardless of which configuration was adopted, and correct the
stale `L2 lambda | 0.003` entry in the Key Hyperparameters table (the actual `config.h` value is
`0.001f`, verified directly this session and used as the anchor for this phase's entire
regularization sweep).

Purpose: ARCH-06 explicitly requires this update "independente do resultado" (regardless of
outcome) — the project's own stated anti-pattern-avoidance discipline (never claim an
architecture/regularization choice without a real comparison backing it), directly mirroring
Phase 1's SMOTE-05 precedent (01-04-PLAN.md).
Output: `CLAUDE.md` updated with a corrected hyperparameter value and a new subsection; no other
file touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-RESEARCH.md
@.planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with the Gap 3 (shallow vs deep MLP) A/B outcome and the L2 lambda correction</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (full file — short, read in full)
    .planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-03-SUMMARY.md (the verdict: 12-row summary, adopted architecture/regularization/param count/justification, McNemar p-values, mean_epochs_to_stop observation)
    .planning/phases/01-gap-2-borderline-smote/01-04-PLAN.md (the exact precedent structure to mirror for this documentation pattern)
  </read_first>
  <action>
In `CLAUDE.md`'s "Key Hyperparameters" table, correct the `| L2 lambda | 0.003 |` row to `| L2
lambda | 0.001 |` — this is the actual `include/config.h` value (verified directly this session
and confirmed as the ONLY definition, used in `src/mlp_train.c`), and this phase's entire
regularization sweep (light=0.6x, baseline=1.0x, strong=1.4x multipliers) was built on this real
value, not the stale `0.003` previously documented. Do not change any other row in this table
(class weights, dropout rates, etc. are out of this phase's scope — leave them as-is per the
project's explicit "focus only on SPEC.md's 3 gaps now, other tech debt later" decision).

Using the real verdict recorded in `02-03-SUMMARY.md`, add a new subsection immediately after
the existing "Gap 2 Outcome" subsection (added by Phase 1's 01-04 plan) — or, if that subsection
does not yet exist in the live file for any reason, immediately after "Important Constraints"
and before "Output Files", matching Phase 1's placement precedent — titled `## Gap 3 Outcome —
Shallow vs Deep MLP Comparison (v33)`, containing: (1) the ARCH-01 correction restated once more
for permanence ("Config C [128,64] confirmadamente a producao atual antes desta comparacao,
correcao ja aplicada no Plano 02-01"); (2) a condensed version of the 12-row comparison table
(architecture, regularization setting, macro_f1, param count) sourced from `02-03-SUMMARY.md`;
(3) the best-per-architecture summary (which regularization won for each of the 4 architectures);
(4) the 1-SE band bounds and which architecture was `ao` (best overall); (5) the McNemar
p-values of each non-best architecture vs `ao`; (6) the verbatim `DECISAO:` sentence naming the
adopted architecture, regularization setting, and parameter count; (7) the Pitfall 3
`mean_epochs_to_stop` observation (whether deeper/more-regularized configs stopped meaningfully
earlier, or whether no such pattern was observed) — carry this caveat forward explicitly, do not
omit it even if the adopted architecture is the deepest one; (8) explicit file pointers to
`results/train_log_v33_gap3_arch_compare.txt` and `results/arch_compare_comparison.csv`.

If the adopted architecture in `02-03-SUMMARY.md`'s verdict differs from Config C (today's
production `[128,64]`), also update the `### MLP Architecture (config.h)` section's `**Layers**:`
bullet to reflect the newly adopted architecture as the one now recommended by this phase's
comparison (note: actually swapping the compiled default `mlp_init_dynamic()`/`config.h`
constants to match is explicitly OUT OF SCOPE for this phase — ARCH-01..06 require the comparison
and documented decision to exist, not a follow-on migration of production defaults; state this
distinction explicitly in the new subsection so a future phase knows whether that migration is
still pending). If the adopted architecture IS Config C (today's production, at whichever
regularization setting won), state plainly that no production change is required — the existing
compiled defaults already match the adopted choice (or note the specific regularization-setting
delta if a non-baseline regularization won, since that would mean the compiled `config.h`
constants technically differ from the empirically-preferred setting — again, whether to apply
that delta to `config.h` is a decision for a future phase, not silently done here).

Do not alter any other section of `CLAUDE.md` (Build & Run Commands, Architecture, Current Best
Results, Gap 2 Outcome, etc. stay untouched by this plan, aside from the one Key Hyperparameters
correction above).
  </action>
  <acceptance_criteria>
    - `grep -c "L2 lambda | 0.001" CLAUDE.md` outputs `1`
    - `grep -c "L2 lambda | 0.003" CLAUDE.md` outputs `0` (stale value fully replaced)
    - `grep -c "## Gap 3 Outcome" CLAUDE.md` outputs `1`
    - `grep -c "train_log_v33_gap3_arch_compare.txt" CLAUDE.md` outputs at least `1`
    - `grep -c "DECISAO:" CLAUDE.md` outputs at least `1`
    - `grep -c "mean_epochs_to_stop\|Pitfall 3\|para tuning distinto por profundidade" CLAUDE.md` outputs at least `1` (Pitfall 3 caveat present, not silently omitted)
    - `git diff --stat CLAUDE.md` shows exactly 1 file changed, with edits scoped to the Key Hyperparameters table row and the new "Gap 3 Outcome" subsection (and, only if applicable, the MLP Architecture section's Layers bullet)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "## Gap 3 Outcome" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md's Key Hyperparameters L2 lambda value is corrected to the real config.h constant; a new, dated Gap 3 Outcome subsection exists with the 12-arm comparison summary, the 1-SE/McNemar decision, the Pitfall 3 caveat, and file pointers, regardless of which architecture was adopted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documentation content -> future readers (PIBIC committee, future contributors, future Claude sessions) | Inaccurate documentation here directly undermines the "comprovavel por comparacao A/B" core value this entire milestone exists to establish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Repudiation (false/unbacked claim in documentation) | `CLAUDE.md`'s Gap 3 Outcome subsection and Key Hyperparameters table | mitigate | This task requires the edit to be sourced directly from `02-03-SUMMARY.md`'s recorded verdict (itself sourced from a real, freshly executed 12-arm run in Plan 02-03) and from a direct `config.h` value verification — never asserted independently of that evidence chain |
| T-02-SC | Tampering (supply chain) | N/A | accept | No package-manager installs in this phase; this is a documentation-only plan |

</threat_model>

<verification>
`grep -c "## Gap 3 Outcome" CLAUDE.md` == 1. The L2 lambda correction is present. The Pitfall 3 caveat is explicitly stated. `git diff CLAUDE.md` touches only the intended sections.
</verification>

<success_criteria>
- CLAUDE.md's Gap 3 (shallow vs deep MLP) claim is accurate and evidence-backed, regardless of which architecture was adopted.
- The stale L2 lambda documentation error is corrected.
- A dated Gap 3 Outcome subsection exists with concrete numbers, the 1-SE/McNemar justification, the Pitfall 3 caveat, and file pointers, satisfying ARCH-06.
</success_criteria>

<output>
Create `.planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-04-SUMMARY.md` when done
</output>
