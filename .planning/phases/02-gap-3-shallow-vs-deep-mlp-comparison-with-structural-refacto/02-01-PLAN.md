---
phase: 02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [CLAUDE.md, include/config.h, include/mlp.h, src/mlp.c, include/mlp_train.h, src/mlp_train.c, src/main.c]
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "CLAUDE.md documents the current production network as Config C [128,64] (2 hidden layers), not Config A [128], before any comparison code exists — correcting SPEC.md line 233's 'A (atual, rasa)' mislabeling in all NEW documentation (SPEC.md itself is a historical artifact and is not edited)"
    - "MLP can represent Config D (3 hidden layers, 4 total layers) via Layer layers[MLP_MAX_LAYERS] with zero out-of-bounds writes, verified by an ad hoc ASan run (not a permanent Makefile target)"
    - "mlp_backward()'s delta buffer is sized from the actual widest hidden layer of the network instance being backpropagated, not a hardcoded MLP_HIDDEN1_SIZE=128 constant"
    - "mlp_train() accepts L2 lambda as a runtime parameter (not the hardcoded L2_LAMBDA macro internally) with zero behavior change for the one existing caller, which explicitly passes L2_LAMBDA"
  artifacts:
    - path: "CLAUDE.md"
      provides: "ARCH-01 config-label correction (Config C = current production, not Config A)"
    - path: "include/config.h"
      provides: "MLP_MAX_LAYERS constant (5), MLP_NUM_LAYERS unchanged (3, still used by mlp_init_dynamic's wrapper)"
    - path: "include/mlp.h"
      provides: "Layer layers[MLP_MAX_LAYERS]; mlp_init_multi()/mlp_count_params() declarations"
    - path: "src/mlp.c"
      provides: "mlp_init_multi() implementation, mlp_init_dynamic() as thin wrapper, dynamic mlp_backward() max_size, mlp_count_params()"
    - path: "include/mlp_train.h + src/mlp_train.c"
      provides: "l2_lambda runtime parameter on mlp_train(), MLP_MAX_LAYERS-sized checkpoint/SWA buffers"
  key_links:
    - from: "mlp_init_multi()"
      to: "Layer layers[MLP_MAX_LAYERS]"
      via: "for (i=0; i<net->num_layers; i++) layer_init(&net->layers[i], ...) with num_layers up to 4 (Config D)"
      pattern: "MLP_MAX_LAYERS"
    - from: "src/main.c's 2 mlp_train() call sites"
      to: "L2_LAMBDA passed explicitly as the new argument"
      via: "signature widening, zero behavior change"
      pattern: "mlp_train\\(&net_master\\[v\\].*L2_LAMBDA"
---

<objective>
Fix the two structural array-sizing hazards RESEARCH.md identified (Pitfalls 1-2) so the MLP struct and its training loop can safely represent Config D (3 hidden layers) with no out-of-bounds writes, generalize `mlp_init_dynamic()` into `mlp_init_multi()` (configurable hidden sizes/dropout rates), parameterize L2 lambda as a runtime argument to `mlp_train()` (prerequisite for Plan 02-02's regularization sweep), and correct the SPEC.md-inherited "Config A = current production" mislabeling in `CLAUDE.md` before any comparison code is written (ARCH-01).

Purpose: This is the prerequisite structural refactor everything else in this phase depends on — Plan 02-02's orchestration layer (`ArchConfig`/`RegSetting`/`mode_arch_compare()`) cannot exist until the MLP core can safely hold 4 different architectures (including a 3-hidden-layer one) and accept a runtime L2 lambda, and no comparison report should ever be produced while `CLAUDE.md` still implies the current 2-hidden-layer production network is "the shallow baseline."
Output: `include/config.h`/`include/mlp.h`/`src/mlp.c`/`include/mlp_train.h`/`src/mlp_train.c` compile cleanly with the generalized, runtime-safe architecture; `src/main.c`'s 2 existing `mlp_train()` call sites are updated minimally (explicit `L2_LAMBDA` argument) to keep the build green — Plan 02-02 replaces these same 2 lines again with the swept `eff_l2` value; `CLAUDE.md` carries the ARCH-01 correction. An ad hoc `-fsanitize=address` smoke test (not committed, not a Makefile target) verifies Config D's array-sizing fix before Plan 02-03's real multi-hour sweep runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-RESEARCH.md
@.planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Exact current state read in full this session. Re-read the live files before editing --
     these are the stable identifiers/line anchors, not a promise line numbers won't shift. -->

Current `include/mlp.h` struct (lines 61-66):
`typedef struct { Layer layers[MLP_NUM_LAYERS]; int num_layers; int timestep; } MLP;`
Declaration to generalize sits at line 77: `void mlp_init_dynamic(MLP *net, int input_size, int output_size);`

Current `include/config.h` architecture block (lines 75-90): `MLP_HIDDEN1_SIZE 128`,
`MLP_HIDDEN2_SIZE 64`, `MLP_HIDDEN3_SIZE 32`, `MLP_NUM_LAYERS 3`; `DROPOUT_RATE_HIDDEN1 0.5f`,
`DROPOUT_RATE_HIDDEN2 0.4f`, `DROPOUT_RATE_HIDDEN3 0.0f` (dead value, never reached — leave
untouched, do NOT change to 0.3; Config D's baseline 0.3 dropout is a Plan 02-02 `main.c`
literal per the user's explicit decision, not a `config.h` value). `L2_LAMBDA` (line 102) is
`0.001f` — verified via direct grep this session as the ONLY definition, used exactly once in
`src/mlp_train.c:255` (`mlp_l2_regularization(net, L2_LAMBDA)`) and once in `src/logreg.c:149`
(untouched by this phase). Note: `CLAUDE.md`'s "Key Hyperparameters" table currently claims
`L2 lambda | 0.003` — this is STALE/INCORRECT against the actual `config.h` value; Plan 02-04
corrects this line when it documents the Gap 3 outcome. This plan's own regularization-baseline
reasoning (consumed by Plan 02-02) MUST use the real `0.001f`, never the stale `0.003`.

Current `src/mlp.c` `mlp_init_dynamic()` (lines 220-241) uses `#if MLP_NUM_LAYERS == 3 ... #else
... #endif` to pick between a 2-hidden-layer and (dead, never-compiled) 3-hidden-layer sizes
array. Current `mlp_backward()` (line 287): `int max_size = MLP_HIDDEN1_SIZE;` — the exact
Pitfall 2 hazard.

Current `include/mlp_train.h` `mlp_train()` declaration (lines 46-50):
`int mlp_train(MLP *net, const float *train_x, const int *train_y, int n_train, const float
*val_x, const int *val_y, int n_val, int num_features, int num_classes, const float
*class_weights, TrainHistory *history);`

Current `src/mlp_train.c` (lines 183-185, 204 — the 6 `MLP_NUM_LAYERS`-sized pointer-array
declarations to widen): `float *best_weights[MLP_NUM_LAYERS], *best_biases[MLP_NUM_LAYERS];`
`float *best_bn_gamma[MLP_NUM_LAYERS], *best_bn_beta[MLP_NUM_LAYERS];`
`float *best_bn_mean[MLP_NUM_LAYERS], *best_bn_var[MLP_NUM_LAYERS];` and (line 204)
`float *swa_weights[MLP_NUM_LAYERS], *swa_biases[MLP_NUM_LAYERS];`. Every loop that fills/frees
these already iterates by `net->num_layers` (a runtime field) — zero loop-body changes needed,
only the declaration widths. Line 255: `batch_loss += mlp_l2_regularization(net, L2_LAMBDA);`
is the one internal reference to the macro that must become a parameter reference.

Current `src/main.c`'s 2 `mlp_train()` call sites (inside `mode_train_ex()`'s per-vowel loop):
`mlp_train(&net_master[v], os_m_x, os_m_y, os_n_m, vl_x_v, vl_y_bin, fold->n_val, nf_vowel, 2,
cw_binary, &h_m);` and `mlp_train(&net_expert[v], os_e_x, os_e_y, os_n_e, ex_vl_x, ex_vl_y,
n_ex_vl, nf_vowel, 4, cw_expert, &h_e);` — both currently omit any L2 argument (it was an
internal macro reference). This plan inserts `L2_LAMBDA` as the new argument at both sites,
immediately before `&h_m`/`&h_e`; Plan 02-02 replaces `L2_LAMBDA` with a computed `eff_l2`
variable at these same 2 lines.

SPEC.md line 233 (read-only, historical, NOT edited by this plan): `| A (atual, rasa) | [128] |
~11k | 0,5 |` — this is the exact mislabeling being corrected in all NEW documentation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ARCH-01 — correct the Config A/C mislabeling in CLAUDE.md before any comparison code exists</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (full file — short, read in full)
    SPEC.md lines 195-265 (GAP 3 section — read-only, source of the mislabeling; do not edit this file)
    .planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-RESEARCH.md (Summary paragraph 1; Pattern 1)
  </read_first>
  <action>
Locate `CLAUDE.md`'s `### MLP Architecture (config.h)` section. Add one clarifying bullet
immediately after the existing `**Layers**:` bullet, stating explicitly (Portuguese, matching
the file's existing bilingual style — headings/prose are already a mix of English structure and
Portuguese technical content is fine, follow the surrounding bullets' tone): the current
production network (2 hidden layers, `[128, 64]`) is Gap 3's "Config C" per the comparison this
phase performs — it is NOT the shallow "Config A [128]" that `SPEC.md`'s original GAP 3 section
(line 233) assumed was in production; no shallow single-hidden-layer configuration has ever
actually been trained in production. State this as a factual correction, not a hedge (e.g. "A
producao atual (2 camadas ocultas [128,64]) e a 'Config C' da comparacao da Fase 2/Gap 3 -- NAO
a 'Config A [128]' que o SPEC.md original assumia estar em producao; nenhuma configuracao rasa
de 1 camada oculta jamais rodou em producao").

Do not touch `SPEC.md` (historical artifact, per project convention — corrections land only in
CLAUDE.md/code comments/the Gap 3 report going forward, never by editing the original SPEC
document). Do not touch any other `CLAUDE.md` section in this task (no Gap 3 outcome section yet
— that is Plan 02-04's job, after real results exist).
  </action>
  <acceptance_criteria>
    - `grep -c "Config C" CLAUDE.md` outputs at least `1`
    - `grep -c "NAO.*Config A\|nao.*Config A\|not.*Config A" CLAUDE.md` outputs at least `1` (explicit correction present, not just a bare mention)
    - `git diff --stat CLAUDE.md` shows exactly 1 file changed, with the edit scoped to the `### MLP Architecture (config.h)` section only
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Config C" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md explicitly states the current production network is Config C [128,64], not Config A, correcting SPEC.md's original assumption in all forward-looking documentation, before any comparison code exists.</done>
</task>

<task type="auto">
  <name>Task 2: ARCH-02 core — MLP_MAX_LAYERS, mlp_init_multi(), mlp_count_params(), dynamic mlp_backward() sizing</name>
  <files>include/config.h, include/mlp.h, src/mlp.c</files>
  <read_first>
    include/config.h (full)
    include/mlp.h (full)
    src/mlp.c (full, 600 lines — already read this session, re-read only if context was cleared)
    .planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-RESEARCH.md (Pattern 2, Pattern 3, Pitfalls 1-2, Code Examples > mlp_count_params)
    .planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-PATTERNS.md (include/mlp.h and src/mlp.c sections)
  </read_first>
  <action>
In `include/config.h`, inside the existing `/* ========== Arquitetura MLP ========== */` banner
block, add `#define MLP_MAX_LAYERS 5` immediately after `#define MLP_NUM_LAYERS 3` (line 81).
`MLP_MAX_LAYERS=5` covers Config D's exact 4 layers (3 hidden + 1 output) with one slot of
headroom, per RESEARCH.md Assumption A3. Keep `MLP_NUM_LAYERS=3` defined and unchanged — it is
still used by `mlp_init_dynamic()`'s compatibility-wrapper role.

In `include/mlp.h`: change `Layer layers[MLP_NUM_LAYERS];` (line 63) to
`Layer layers[MLP_MAX_LAYERS];` — this is the only struct field that changes; `num_layers`/
`timestep` remain unchanged runtime fields, and every existing loop in this codebase already
iterates by `net->num_layers`, never the macro (verified this session across `mlp_forward`,
`mlp_backward`, `mlp_zero_gradients`, `mlp_clip_gradients`, `mlp_adam_update`,
`mlp_l2_regularization`, `mlp_save_checkpoint`/`mlp_load_checkpoint`, `mlp_save`/`mlp_load`,
`mlp_free`). Add two new declarations immediately after the existing `mlp_init_dynamic()`
declaration (line 77), matching this file's existing Portuguese doc-comment house style (see
the doc comment directly above `mlp_init_dynamic()` for the exact tone to match):
`void mlp_init_multi(MLP *net, int input_size, int output_size, const int *hidden_sizes, int
n_hidden, const float *dropout_rates);` (doc comment: "Inicializa MLP com arquitetura
configuravel: hidden_sizes[n_hidden] define a largura de cada camada oculta,
dropout_rates[n_hidden] a taxa de dropout correspondente. net->num_layers e definido
internamente como (n_hidden + 1).") and `int mlp_count_params(const MLP *net);` (doc comment:
"Retorna o numero total de parametros treinaveis (pesos + biases) somados sobre todas as
net->num_layers camadas. BN esta desabilitado em todo o pipeline atual (use_bn=0 em todas as
chamadas de layer_init), portanto nao contribui.").

In `src/mlp.c`: implement `mlp_init_multi()` (place it immediately before `mlp_init_dynamic()`).
Body: set `net->num_layers = n_hidden + 1;` and `net->timestep = 0;`; build a local `int
sizes[MLP_MAX_LAYERS + 1];` array with `sizes[0] = input_size;`, a loop copying
`hidden_sizes[0..n_hidden-1]` into `sizes[1..n_hidden]`, and `sizes[n_hidden + 1] =
output_size;`; loop `i` from `0` to `net->num_layers - 1` calling `layer_init(&net->layers[i],
sizes[i], sizes[i + 1], 0)` (hardcode `use_bn=0` directly — BN is disabled everywhere in this
codebase; do not add a `use_bn` parameter, do not resurrect BN wiring) and set
`net->layers[i].dropout_rate = (i < n_hidden) ? dropout_rates[i] : 0.0f;` (output layer always
gets 0 dropout, matching today's behavior).

Rewrite `mlp_init_dynamic()` to become a thin 2-statement wrapper, removing the existing `#if
MLP_NUM_LAYERS == 3 ... #else ... #endif` branching entirely (the `#else` branch was dead code
— `MLP_NUM_LAYERS` is permanently `3` — removing it is safe and required, since the
compile-time layer-count selector is now superseded by `mlp_init_multi()`'s runtime
`n_hidden` parameter; `MLP_NUM_LAYERS` itself stays defined in `config.h`, only this one `#if`
use is removed): `int hidden[] = { MLP_HIDDEN1_SIZE, MLP_HIDDEN2_SIZE }; float drop[] = {
DROPOUT_RATE_HIDDEN1, DROPOUT_RATE_HIDDEN2 }; mlp_init_multi(net, input_size, output_size,
hidden, 2, drop);` — this reproduces today's exact behavior byte-for-byte (2 hidden layers,
`[128, 64]`, dropout `[0.5, 0.4]`).

Fix `mlp_backward()`'s stale sizing (line 287, RESEARCH.md Pitfall 2): replace `int max_size =
MLP_HIDDEN1_SIZE;` with a loop computed from the actual network instance: `int max_size = 0; for
(int i = 0; i < net->num_layers - 1; i++) { if (net->layers[i].output_size > max_size) max_size
= net->layers[i].output_size; }` (excludes the output layer, matching the existing intent — the
`delta`/`delta_next` buffers only ever hold hidden-layer-sized data). Place this immediately
before the existing `delta`/`delta_next` `safe_malloc` calls; do not change anything else in
`mlp_backward()`.

Add `mlp_count_params()` near the end of the file (after `mlp_l2_regularization()` or near
`mlp_free()`): loop `net->num_layers`, summing `l->output_size * l->input_size + l->output_size`
per layer (matches `layer_init()`'s exact allocation formula), return the total `int`.
  </action>
  <acceptance_criteria>
    - `grep -c "#define MLP_MAX_LAYERS 5" include/config.h` outputs `1`
    - `grep -c "Layer layers\[MLP_MAX_LAYERS\];" include/mlp.h` outputs `1`
    - `grep -c "Layer layers\[MLP_NUM_LAYERS\];" include/mlp.h` outputs `0` (old declaration fully replaced)
    - `grep -c "void mlp_init_multi" include/mlp.h` outputs `1`
    - `grep -c "int mlp_count_params" include/mlp.h` outputs `1`
    - `grep -c "^void mlp_init_multi" src/mlp.c` outputs `1`
    - `grep -c "^int mlp_count_params" src/mlp.c` outputs `1`
    - `grep -c "#if MLP_NUM_LAYERS == 3" src/mlp.c` outputs `0` (dead branching removed)
    - `grep -c "int max_size = MLP_HIDDEN1_SIZE;" src/mlp.c` outputs `0` (stale sizing replaced)
    - `grep -A3 "int max_size = 0;" src/mlp.c | grep -c "net->layers\[i\].output_size"` outputs at least `1`
    - `make 2>&1 | tee /tmp/build_02_01_t2.log; grep -c "error:" /tmp/build_02_01_t2.log` outputs `0`
  </acceptance_criteria>
  <verify>
    <automated>make 2>&1 | tee /tmp/build_02_01_t2.log; test "$(grep -c 'error:' /tmp/build_02_01_t2.log)" = "0"</automated>
  </verify>
  <done>MLP_MAX_LAYERS is defined and used for the Layer array; mlp_init_multi() builds a runtime-configurable-depth network reused by mlp_init_dynamic()'s now-thin wrapper; mlp_backward()'s delta buffer sizing is computed dynamically from the actual network instance; mlp_count_params() exists; file compiles cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: mlp_train.c/mlp_train.h widening + l2_lambda runtime parameter + ASan Config D smoke test</name>
  <files>include/mlp_train.h, src/mlp_train.c, src/main.c</files>
  <read_first>
    include/mlp_train.h (full)
    src/mlp_train.c (full, 349 lines — already read this session)
    src/main.c (lines 436-493 — the 2 mlp_train() call sites inside mode_train_ex()'s per-vowel loop)
    .planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-RESEARCH.md (Pitfall 1, "Alternatives Considered" — regularization SE source; the l2_lambda parameterization itself is a NEW requirement from the user's regularization-sweep decision, not literally spelled out in RESEARCH.md — apply the same "widen the signature, keep call sites explicit" discipline RESEARCH.md uses elsewhere)
  </read_first>
  <action>
In `include/mlp_train.h`, widen `mlp_train()`'s declaration: insert `float l2_lambda,` as a new
parameter positioned immediately before the final `TrainHistory *history` parameter (i.e.,
after `const float *class_weights,` and before `TrainHistory *history`). Update the doc comment
above the declaration to mention the new parameter ("l2_lambda: coeficiente L2 desta chamada de
treino (permite variar a forca de regularizacao por configuracao de arquitetura, Gap 3)").

In `src/mlp_train.c`: update `mlp_train()`'s function definition parameter list to match
(insert `float l2_lambda` at the same position). Replace the one internal reference to the
macro, `batch_loss += mlp_l2_regularization(net, L2_LAMBDA);` (line 255), with `batch_loss +=
mlp_l2_regularization(net, l2_lambda);` — this is the only behavioral-wiring change in this
file; `L2_LAMBDA` remains defined in `config.h` (still used by `src/logreg.c`, untouched) but is
no longer referenced directly inside `mlp_train.c`.

Widen the 6 stack-declared pointer-array declarations from `MLP_NUM_LAYERS` to `MLP_MAX_LAYERS`
(lines 183-185 and 204): `float *best_weights[MLP_MAX_LAYERS], *best_biases[MLP_MAX_LAYERS];`
`float *best_bn_gamma[MLP_MAX_LAYERS], *best_bn_beta[MLP_MAX_LAYERS];` `float
*best_bn_mean[MLP_MAX_LAYERS], *best_bn_var[MLP_MAX_LAYERS];` and `float
*swa_weights[MLP_MAX_LAYERS], *swa_biases[MLP_MAX_LAYERS];`. Do NOT change any loop bound in
this file (every fill/cleanup loop already iterates by `net->num_layers`) — only the 4
declaration lines' array widths change.

In `src/main.c`: update the 2 existing `mlp_train()` call sites inside `mode_train_ex()`'s
per-vowel loop to pass the new argument, inserting `L2_LAMBDA` immediately before `&h_m` and
`&h_e` respectively (this is a minimal, behavior-preserving edit — `L2_LAMBDA` is exactly the
value that was already implicitly used; Plan 02-02 replaces `L2_LAMBDA` with a computed
`eff_l2` variable at these same 2 lines once `ArchConfig`/`RegSetting` exist). Do not change
anything else in `main.c` in this task.

ASan Config D smoke test (ad hoc verification per RESEARCH.md Pitfalls 1-2 and the project's
"one-off pre-sweep verification, not a permanent CI target" scoping — REQUIREMENTS.md's Out of
Scope table): create a standalone throwaway `.c` file OUTSIDE `src/`/`include/` (e.g. under
`/tmp/` or another location not tracked by git, so the repo's compile-check hook and `git
status` never see it) that: includes `config.h`/`utils.h`/`mlp.h`/`mlp_train.h` via `-I
include`; calls `rng_seed(42)`; builds 40 synthetic training samples and 10 validation samples
with `num_features=85`, `num_classes=4` using `rng_normal()` for features and `rng_int(4)` for
labels; calls `mlp_init_multi(&net, 85, 4, (int[]){128, 64, 32}, 3, (float[]){0.70f, 0.56f,
0.42f})` — Config D's STRONG-regularization dropout values (the widest/most aggressive test of
the array-sizing fix, per this phase's regularization sweep design); calls `mlp_train(&net,
train_x, train_y, 40, val_x, val_y, 10, 85, 4, NULL, 0.0014f, &history)` (STRONG L2, `NULL`
class weights); calls `mlp_free(&net)`. Compile with `gcc -fsanitize=address -g -O0 -std=c99
-Iinclude {scratch}.c src/mlp.c src/mlp_train.c src/utils.c -lm -o {scratch_binary}` (no OpenMP
needed for this isolated test, omit `-fopenmp`). Run the binary and confirm zero AddressSanitizer
error output and exit code `0`. Delete both the scratch `.c` file and its compiled binary
afterward — nothing from this smoke test is committed to the repository or added to the
Makefile.
  </action>
  <acceptance_criteria>
    - `grep -c "float l2_lambda," include/mlp_train.h` outputs `1`
    - `grep -c "float l2_lambda" src/mlp_train.c` outputs at least `1` (function definition parameter)
    - `grep -c "mlp_l2_regularization(net, l2_lambda);" src/mlp_train.c` outputs `1`
    - `grep -c "mlp_l2_regularization(net, L2_LAMBDA);" src/mlp_train.c` outputs `0` (old macro reference removed)
    - `grep -c "MLP_MAX_LAYERS\]" src/mlp_train.c` outputs `8` (4 declaration lines x 2 array names each: best_weights/best_biases, best_bn_gamma/best_bn_beta, best_bn_mean/best_bn_var, swa_weights/swa_biases)
    - `grep -c "MLP_NUM_LAYERS\]" src/mlp_train.c` outputs `0` (all 6 arrays widened)
    - `grep -c "cw_binary, L2_LAMBDA, &h_m" src/main.c` outputs `1`
    - `grep -c "cw_expert, L2_LAMBDA, &h_e" src/main.c` outputs `1`
    - `make 2>&1 | tee /tmp/build_02_01_t3.log; grep -c "error:" /tmp/build_02_01_t3.log` outputs `0`
    - `grep -c "warning:" /tmp/build_02_01_t3.log` outputs at most `2` (same 2 pre-existing warnings, no new ones)
    - ASan scratch binary run captured in this task's execution: reported exit code `0` and zero lines containing `ERROR: AddressSanitizer` in its output
    - The scratch `.c` file and its compiled binary do not appear in `git status --porcelain` after this task completes
  </acceptance_criteria>
  <verify>
    <automated>make 2>&1 | tee /tmp/build_02_01_t3.log; test "$(grep -c 'error:' /tmp/build_02_01_t3.log)" = "0" && test "$(grep -c 'warning:' /tmp/build_02_01_t3.log)" -le 2</automated>
  </verify>
  <done>mlp_train() accepts l2_lambda as a runtime parameter with zero behavior change for the existing L2_LAMBDA-passing call sites; mlp_train.c's 6 checkpoint/SWA pointer arrays are widened to MLP_MAX_LAYERS; an ad hoc ASan run on Config D's exact widest dropout/L2 combination (STRONG setting) completes with zero memory-safety errors, proving the array-sizing fix before Plan 02-03's real multi-hour sweep; no scratch files are committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Compile-time macro -> runtime struct sizing | `Layer layers[]`'s width moving from a compile-time-fixed `MLP_NUM_LAYERS` to a headroom-sized `MLP_MAX_LAYERS`, with the actually-used depth (`num_layers`) becoming a genuinely runtime-varying value within a single process run (up to 4 different depths across 12 arch-compare arms) for the first time in this codebase's history |
| ASan scratch test -> git working tree | The ad hoc verification binary/source must never be committed or left in a location the compile-check hook scans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (memory corruption) | `Layer layers[MLP_MAX_LAYERS]` sized for Config D's 4 layers | mitigate | Fixed-size headroom array (`MLP_MAX_LAYERS=5`, 1 slot of margin over Config D's exact 4) + a dedicated ASan smoke test on Config D's exact widest dropout/L2 combination before any real training data is used, per RESEARCH.md Pitfalls 1-2 |
| T-02-02 | Tampering (memory corruption) | `mlp_backward()`'s delta buffer, previously sized from a stale `MLP_HIDDEN1_SIZE` constant | mitigate | Dynamic `max_size` computed from `net->layers[i].output_size` at call time — correct for any future architecture, not coincidentally-safe-today |
| T-02-03 | Repudiation (documentation drift) | `CLAUDE.md`'s config-label claim | mitigate | ARCH-01 correction applied as Task 1, before any comparison code exists, directly contradicting SPEC.md's non-edited historical mislabeling |
| T-02-SC | Tampering (supply chain) | N/A | accept | No package-manager installs in this phase — pure C99, zero external dependencies (RESEARCH.md "Package Legitimacy Audit: N/A") |

</threat_model>

<verification>
`make` compiles cleanly (0 errors, at most 2 pre-existing warnings) after each task. All grep-based acceptance criteria pass. The ad hoc ASan Config D smoke test (Task 3) runs and exits 0 with zero AddressSanitizer errors, then is deleted — this is the one-off pre-sweep verification REQUIREMENTS.md's Out of Scope table permits (not a permanent `make asan` target). No full pipeline run happens in this plan — that is Plan 02-03's job, after Plan 02-02's orchestration layer exists.
</verification>

<success_criteria>
- `CLAUDE.md` correctly labels the current production network as Config C, before any comparison exists (ARCH-01).
- `MLP` can safely represent Config D (3 hidden layers) via `MLP_MAX_LAYERS`-sized arrays in both `mlp.h` and `mlp_train.c`, verified by an ad hoc ASan run (ARCH-02).
- `mlp_init_multi()` generalizes `mlp_init_dynamic()` (kept as a behavior-identical thin wrapper); `mlp_backward()`'s buffer sizing is dynamic, not coincidentally-safe (ARCH-02).
- `mlp_train()` accepts L2 lambda as a runtime parameter, unblocking Plan 02-02's regularization sweep, with zero behavior change for the existing caller.
</success_criteria>

<output>
Create `.planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-01-SUMMARY.md` when done
</output>
