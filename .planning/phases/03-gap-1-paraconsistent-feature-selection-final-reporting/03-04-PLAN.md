---
phase: 03-gap-1-paraconsistent-feature-selection-final-reporting
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - results/train_log_v34_gap1_paraconsistent_ab.txt
  - results/train_log_v34_gap1_paraconsistent_ab_console.txt
  - results/paraconsistent_ab_comparison.csv
  - results/paraconsistent_selection_freq.csv
  - results/gap_adoption_status.csv
  - "models/selected_master_fold{0-4}_v{0-2}.bin (15 files)"
  - "models/selected_expert_fold{0-4}_v{0-2}.bin (15 files)"
autonomous: true
requirements: [PARA-05]

must_haves:
  truths:
    - "A real (not simulated) 2-arm A/B comparison (without vs with paraconsistent selection) has been executed under the SMOTE_BORDERLINE + Config C baseline configuration, not claimed by code review alone"
    - "The selection-frequency table contains exactly 2550 data rows (85 features x 3 vowels x 5 folds x 2 networks)"
    - "No NaN/Inf values appear anywhere in the produced reports"
    - "Whichever DECISAO branch fired is grounded in the actual off_res/on_res Macro F1 and feature_reduction numbers printed in the same report file, not asserted independently of them"
  artifacts:
    - path: "results/train_log_v34_gap1_paraconsistent_ab.txt"
      provides: "Full text report: CI table, direct McNemar, feature_reduction, DECISAO sentence"
      contains: "DECISAO: Selecao Paraconsistente"
    - path: "results/paraconsistent_selection_freq.csv"
      provides: "Per (fold,vowel,network,feature) mu/lambda/gc/gct/selected rows, 2550 data rows + header"
    - path: "results/gap_adoption_status.csv"
      provides: "3-row CROSS-01 consolidated table with the real Gap 1 decision/delta/mcnemar-p/citation"
  key_links:
    - from: "./build/vocal_detect paraconsistent-ab ."
      to: "results/train_log_v34_gap1_paraconsistent_ab.txt"
      via: "mode_paraconsistent_ab() -> write_gap1_report() / write_gap_adoption_status()"
      pattern: "DECISAO: Selecao Paraconsistente"
---

<objective>
Execute the real, end-to-end paraconsistent-selection A/B comparison (`./build/vocal_detect paraconsistent-ab .`) built in Plans 03-01/03-02/03-03, producing durable, freshly-generated evidence in `results/` and `models/` for PARA-05: a per-feature selection-frequency table across all 30 (fold x vowel x network) runs, Macro F1 before/after, and a fully mechanical adopt/reject decision.

Purpose: This is the one place in this phase real numbers are generated — no prior plan runs the full pipeline. Everything Plan 03-05's CLAUDE.md documentation states must trace back to the artifacts produced here, not to code-review inference.
Output: `results/train_log_v34_gap1_paraconsistent_ab.txt`, `results/paraconsistent_ab_comparison.csv`, `results/paraconsistent_selection_freq.csv`, `results/gap_adoption_status.csv`, and 30 `models/selected_{master,expert}_fold{0-4}_v{0-2}.bin` persistence files, all freshly generated and cross-checked for internal consistency and absence of NaN/Inf.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-RESEARCH.md
@.planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-03-SUMMARY.md
@results/metrics_global_borderline_C_baseline.csv
@CLAUDE.md

<critical_process_note>
This is a 2-arm comparison (like Phase 1's `smote-ab`), not a 12-arm sweep (like Phase 2's
`arch-compare`) — expect roughly the same duration class as `smote-ab` (~60-180 minutes total for
both arms combined, per CLAUDE.md's documented single-pipeline-run timing of ~30-90 min each),
NOT the 6-18+ hour class of `arch-compare`. Still: detach the process (`nohup`/`setsid`), redirect
output with `tee` (not just `>`) so progress is visible, and ACTIVELY POLL TO COMPLETION IN-TURN —
do not end the turn while the process is running, even across many `run_in_background`/Monitor
cycles or repeated Bash-tool foreground-timeout re-arms. This is the exact pattern Phase 0's
`00-01`/`00-03`, Phase 1's `01-03`, and Phase 2's `02-03` plans already used successfully. If the
Bash tool's foreground timeout is hit while polling, immediately re-arm a new
`run_in_background` monitor (e.g. `pgrep`-based: `while pgrep -f "vocal_detect paraconsistent-ab"
> /dev/null; do sleep 120; done`) rather than ending the turn. Do NOT fire-and-forget.
</critical_process_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the real paraconsistent-ab A/B comparison to completion</name>
  <files>none (runtime execution only)</files>
  <read_first>
    src/main.c (confirm mode_paraconsistent_ab()/write_gap1_report()/write_gap_adoption_status()/paraconsistent-ab dispatch exist and match Plan 03-03's SUMMARY before starting the run)
    .planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-03-SUMMARY.md
  </read_first>
  <action>
Confirm prerequisites: `results/`/`models/` directories exist (`mkdir -p results models`); `make
clean && make` builds `build/vocal_detect` with zero errors against the final Plan-03 source
(a fresh build, not a stale incremental one, since Plans 03-01/02/03 touched multiple files); the
working directory has the gitignored SVD audio directories and `overview_merged.csv` present (per
CLAUDE.md Prerequisites) — if absent, report this task as blocked, do not work around it. Confirm
`results/features.csv` exists and is column-count-valid for `TOTAL_FEATURES=251` (reuse the cache
if present, avoiding a redundant ~42s re-extraction — `features_load_csv()` self-validates this).

Run `./build/vocal_detect paraconsistent-ab . 2>&1 | tee
results/train_log_v34_gap1_paraconsistent_ab_console.txt` as a detached, polled background
process per the `critical_process_note` above. Wait for the process to exit. Confirm exit code
`0`.

After completion, confirm all of the following exist and are non-empty:
`results/train_log_v34_gap1_paraconsistent_ab.txt`, `results/paraconsistent_ab_comparison.csv`,
`results/paraconsistent_selection_freq.csv`, `results/gap_adoption_status.csv`, and 30 model
files matching `models/selected_master_fold{0-4}_v{0-2}.bin` /
`models/selected_expert_fold{0-4}_v{0-2}.bin` (these come only from the `PARA_SELECT_ON` arm,
per Plan 03-02's design — the `PARA_SELECT_OFF` arm writes none, by construction).
  </action>
  <acceptance_criteria>
    - Process exit code `0` (captured via `echo "exit:$?"` immediately after the run completes)
    - `test -s results/train_log_v34_gap1_paraconsistent_ab.txt && echo present` outputs `present`
    - `grep -c "DECISAO: Selecao Paraconsistente" results/train_log_v34_gap1_paraconsistent_ab.txt` outputs `1` (exactly one decision line)
    - `test -s results/paraconsistent_ab_comparison.csv && echo present` outputs `present`
    - `test -s results/paraconsistent_selection_freq.csv && echo present` outputs `present`
    - `test -s results/gap_adoption_status.csv && echo present` outputs `present`
    - `ls models/selected_master_fold*_v*.bin models/selected_expert_fold*_v*.bin 2>/dev/null | wc -l` outputs `30`
  </acceptance_criteria>
  <verify>
    <automated>test -s results/train_log_v34_gap1_paraconsistent_ab.txt && grep -c "DECISAO: Selecao Paraconsistente" results/train_log_v34_gap1_paraconsistent_ab.txt</automated>
  </verify>
  <done>./build/vocal_detect paraconsistent-ab . ran to completion (exit 0) and produced all expected artifacts: the text report with exactly one DECISAO sentence, the comparison CSV, the 2550-row-plus-header selection-frequency CSV, the 3-row Gap Adoption Status CSV, and all 30 per-(fold,vowel,network) persisted selection-index files.</done>
</task>

<task type="auto">
  <name>Task 2: Verify report correctness, internal consistency, and the CLAUDE.md regression floor</name>
  <files>none (verification/inspection only)</files>
  <read_first>
    results/train_log_v34_gap1_paraconsistent_ab.txt (just produced)
    results/paraconsistent_ab_comparison.csv
    results/paraconsistent_selection_freq.csv
    results/gap_adoption_status.csv
    CLAUDE.md (Constraints section — "Reverter ou manter apenas como experimento documentado qualquer mudança que derrube o Macro F1 global abaixo de 0,42")
  </read_first>
  <action>
Read the produced `train_log_v34_gap1_paraconsistent_ab.txt` and confirm: (a) the DECISAO sentence
matches one of the 3 fixed branches implemented in Plan 03-03 Task 1, and is arithmetically
consistent with the actual `off_res`/`on_res` Macro F1 numbers printed in the same file (reproduce
the branch condition by hand from the raw numbers — do not trust the sentence at face value); (b)
the direct McNemar chi2/p between the two arms is reported; (c) neither arm's Macro F1 falls below
CLAUDE.md's documented regression floor of 0.42 — if it does, this must be flagged explicitly as
a finding for Plan 03-05's documentation (not silently accepted, and not treated as a blocking
failure of this plan either — the SPEC.md acceptance rule, not this floor, governs the adopt/reject
decision itself; the floor is a separate project-wide constraint that must not be silently
violated without comment).

Cross-check `results/paraconsistent_selection_freq.csv`: confirm the row count is exactly `1 +
2550` (header + 85 features x 3 vowels x 5 folds x 2 networks); confirm the `selected` column
(9th field) is `0` or `1` only; confirm no row's `mu`/`lambda`/`gc`/`gct` fields contain `nan` or
`inf` (case-insensitive).

Confirm `results/gap_adoption_status.csv` has exactly 4 lines (header + 3 gap rows) and the Gap 1
row's citation-status field matches the CROSS-02 rule (cites PAL2v iff the Gap 1 decision string
starts with `ADOTAD`, otherwise states the technique is not active).

Record the final verdict (off_res/on_res Macro F1 + CI, direct McNemar chi2/p, mean_n_selected,
feature_reduction, the DECISAO sentence and which of the 3 branches fired, the 0.42-floor check
result, and the Gap Adoption Status table's 3 rows) explicitly in this plan's own SUMMARY.md so
Plan 03-05 can consume it directly without re-deriving anything.
  </action>
  <acceptance_criteria>
    - `wc -l < results/paraconsistent_selection_freq.csv` outputs `2551`
    - `grep -ci "nan\|inf" results/paraconsistent_selection_freq.csv` outputs `0`
    - `awk -F, 'NR>1 && $9!=0 && $9!=1' results/paraconsistent_selection_freq.csv | wc -l` outputs `0`
    - `wc -l < results/gap_adoption_status.csv` outputs `4`
    - `grep -ci "nan\|inf" results/train_log_v34_gap1_paraconsistent_ab.txt results/paraconsistent_ab_comparison.csv results/gap_adoption_status.csv` outputs `0` for all 3 files
    - The 0.42 Macro F1 floor check result (pass, or documented exception) is explicitly recorded in this task's SUMMARY output — not silently skipped
    - The DECISAO branch is manually reproduced from the raw off_res/on_res numbers and confirmed to match the printed sentence
  </acceptance_criteria>
  <verify>
    <automated>grep -ci "nan\|inf" results/paraconsistent_selection_freq.csv results/train_log_v34_gap1_paraconsistent_ab.txt</automated>
  </verify>
  <done>The produced Gap 1 A/B report is verified internally consistent (DECISAO sentence matches the raw numbers by hand-reproduction), the selection-frequency table has the exact expected row count with no NaN/Inf and a valid 0/1 selected column, the Gap Adoption Status table's CROSS-02 citation rule is confirmed correct, and the CLAUDE.md 0.42 regression floor is explicitly checked (not silently skipped) regardless of outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Long-running foreground process -> conversation turn boundary | Same class of risk as Phase 0/1/2's precedent runs, though this run's expected duration (~60-180 min) is materially shorter than Phase 2's 6-18+ hour sweep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Denial of Service (self-inflicted, process abandonment) | The `paraconsistent-ab` background run | mitigate | Follow the exact `nohup`/`tee`/poll-to-completion pattern proven in Phase 0/1/2's execution plans |
| T-03-10 | Repudiation (unverifiable/cherry-picked decision) | The DECISAO sentence and the Gap Adoption Status table | mitigate | Task 2 mechanically reproduces the fixed decision branch by hand from the raw off_res/on_res numbers rather than trusting the report's prose at face value, and explicitly checks the CLAUDE.md 0.42 regression floor rather than silently skipping it |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — zero external package installs in this phase |

</threat_model>

<verification>
`./build/vocal_detect paraconsistent-ab .` exits 0 and produces all files listed in this plan's frontmatter. The DECISAO sentence is mechanically consistent with the underlying off_res/on_res numbers (Task 2). The selection-frequency table has exactly 2550 data rows with no NaN/Inf and a valid 0/1 selected column. The CLAUDE.md 0.42 Macro F1 regression floor is explicitly checked for both arms.
</verification>

<success_criteria>
- A real, freshly executed 2-arm paraconsistent-selection comparison exists in `results/`, not a claim.
- The selection-frequency table (PARA-05's core deliverable) is complete, correctly sized, and free of NaN/Inf.
- The adopt/reject decision is verified consistent with its own underlying numbers.
- The Gap Adoption Status table's CROSS-02 citation rule is confirmed correct against the real, live decision.
</success_criteria>

<output>
Create `.planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-04-SUMMARY.md` when done, including the final verdict (off_res/on_res numbers, DECISAO branch, mean_n_selected/feature_reduction, 0.42-floor check, Gap Adoption Status table contents) so Plan 03-05 can consume it directly.
</output>
