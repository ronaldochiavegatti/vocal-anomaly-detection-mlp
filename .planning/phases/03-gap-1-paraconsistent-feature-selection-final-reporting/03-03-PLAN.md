---
phase: 03-gap-1-paraconsistent-feature-selection-final-reporting
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [src/main.c]
autonomous: true
requirements: [PARA-05, CROSS-01, CROSS-02]

must_haves:
  truths:
    - "Gap 1's adopt/reject decision is computed by fixed code logic implementing SPEC.md's exact trade-off rule (equal-or-better Macro F1, OR up to -0.01 worse with >=30% feature reduction) -- never asserted by inspection"
    - "The consolidated Gap Adoption Status table cites PAL2v (Da Costa 1990 / Abe & Nakamatsu 2009) only if Gap 1 is actually adopted -- the citation string branches on the live decision variable, not a hardcoded assumption"
    - "The paraconsistent-ab CLI mode runs both arms (without/with selection) fixed at the already-adopted Gap 2/Gap 3 configuration (SMOTE_BORDERLINE, Config C, REG_BASELINE) -- never mode_train()'s unmodified SMOTE_STANDARD default"
  artifacts:
    - path: "src/main.c"
      provides: "write_gap1_report(), write_gap_adoption_status(), mode_paraconsistent_ab(), paraconsistent-ab CLI dispatch registration"
  key_links:
    - from: "mode_paraconsistent_ab()"
      to: "mode_train_ex()"
      via: "both arms pass SMOTE_BORDERLINE, &ARCH_CONFIGS[2], REG_BASELINE, varying only PARA_SELECT_OFF/PARA_SELECT_ON"
      pattern: "SMOTE_BORDERLINE, &ARCH_CONFIGS\\[2\\], REG_BASELINE, PARA_SELECT_"
    - from: "write_gap_adoption_status()'s Gap 1 citation field"
      to: "decision_gap1 (the live write_gap1_report() outcome)"
      via: "if/else branching on the actual adoption outcome, never a hardcoded citation string"
      pattern: "decision_gap1"
---

<objective>
Implement `write_gap1_report()` (SPEC.md's Gap 1 acceptance rule as fixed code logic, PARA-05), `write_gap_adoption_status()` (the 3-row consolidated Gap/Decision/Delta/McNemar-p/Citation table, CROSS-01, with a citation field that branches on the live Gap 1 outcome per CROSS-02), and `mode_paraconsistent_ab()` (the new CLI comparison mode, mirroring `mode_smote_ab()`/`mode_arch_compare()` exactly).

Purpose: This is the reporting/orchestration layer that turns Plan 03-01's algorithm and Plan 03-02's integration into an actual, runnable A/B comparison with a mechanically-verifiable adoption decision — matching this project's established discipline (Gap 2/Gap 3 both compute their adopt/reject decisions in code, never by inspection) and closing CROSS-02's exact risk (citing a technique that isn't actually active in the shipped model).
Output: `src/main.c` gains the reporting/CLI layer; `./build/vocal_detect paraconsistent-ab .` is a real, dispatchable CLI mode ready for Plan 03-04 to execute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-RESEARCH.md
@.planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-PATTERNS.md
@.planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-02-SUMMARY.md
@CLAUDE.md
@SPEC.md

<interfaces>
<!-- Exact current state read in full this session (pre-Plan-03-02 edits). Re-read src/main.c
     live before editing -- Plan 03-02 has already shifted every line number cited elsewhere in
     this phase's plans; locate the functions below by name, not by these original line numbers. -->

`write_smote_ab_report()` (original lines 727-813, full function already read this session) is
the exact structural analog for `write_gap1_report()`: per-metric CI table via `%-28s %.4f
[%.4f, %.4f]      %.4f [%.4f, %.4f]` format, a direct `metrics_mcnemar()` call between the two
arms' out-of-fold predictions (valid because both arms share the same seed/fold order via
`kfold_split()`'s internal reseed), a fixed `if`/`else` DECISAO sentence (never narrative), and a
parallel CSV export with `metric,standard,standard_ci_lower,standard_ci_upper,borderline,...`
columns. `write_gap1_report()` copies this shape with `off_res`/`on_res` naming and the SPEC.md
Gap 1 trade-off rule (3 branches, not 2 — see Task 1).

`mode_smote_ab()` (original lines 819-832, full function already read) is the exact structural
analog for `mode_paraconsistent_ab()`: log a long-duration warning, declare 2 zero-initialized
`ABResult`s, call `mode_train_ex()` twice (varying exactly one parameter between calls), call the
report writer, free both results' `y_true`/`y_pred`.

`write_arch_compare_report()` (original lines 839-948) is the closest analog for combining
multiple gap outcomes into one table (`write_gap_adoption_status()`'s CROSS-01 table) — reuse its
`fprintf(f, "col1,col2,...")`-style CSV row-per-decision pattern.

`main()`'s dispatch chain (original lines 1011-1023): `if (strcmp(mode, "smote-ab") == 0) return
mode_smote_ab(base_dir) == 0 ? 0 : 1;` / `if (strcmp(mode, "arch-compare") == 0) return
mode_arch_compare(base_dir) == 0 ? 0 : 1;` — the exact style to copy for `"paraconsistent-ab"`.

SPEC.md lines 123-131 (Gap 1's acceptance criterion, verbatim): "Só substituir a versão sem
seleção se: Macro F1 igual ou superior, ou Macro F1 ligeiramente inferior (até −0.01) mas com
redução de features ≥30%". This plan implements this EXACT rule as fixed `if`/`else if`/`else`
code (RESEARCH.md's Open Question 3 recommendation) — never left as a narrative judgment call in
the report's prose.

`ABResult.mean_n_selected` (Plan 03-02's new field): 0 when the arm ran with `PARA_SELECT_OFF`,
the real average when `PARA_SELECT_ON` — `write_gap1_report()` uses `on_res->mean_n_selected` (the
`PARA_SELECT_ON` arm's value) to compute `feature_reduction`.

`metrics_mcnemar()` signature (`include/metrics.h`, unchanged): `void metrics_mcnemar(const int
*y_true, const int *y_pred_a, const int *y_pred_b, int n_samples, float *chi2_out, float
*p_value_out);`.

`ArchConfig ARCH_CONFIGS[4]` (unchanged, index `[2]` is Config C — today's production
`[128,64]`); `RegSetting REG_BASELINE` (unchanged, multiplier `1.0f`) — both reused verbatim by
`mode_paraconsistent_ab()`, per RESEARCH.md's Pitfall 5 finding: never call `mode_train()` or rely
on its unmodified `SMOTE_STANDARD` default for this comparison.

The already-finalized historical facts for Gap 2/Gap 3 (hardcoded as literal strings in
`write_gap_adoption_status()`'s Gap 2/Gap 3 rows, not recomputed — these are settled, cited
outcomes from CLAUDE.md's existing "Gap 2 Outcome"/"Gap 3 Outcome" sections, already read this
session): Gap 2 = ADOTADO, delta +0.0235 (point estimate) / bootstrap-mean 0.4565 vs 0.4338,
McNemar p=0.6606 (not significant), citation "Han/Wang/Mao 2005 (Borderline-SMOTE1)". Gap 3 =
ADOTADO (Config C, baseline reg — no config.h change since this IS the already-compiled
production config), McNemar p=0.7463 vs Config D (not significant), citation "N/A — comparacao
metodologica interna, nao e uma tecnica externa citavel".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: write_gap1_report() — SPEC.md's Gap 1 acceptance rule as fixed code logic (PARA-05)</name>
  <files>src/main.c</files>
  <read_first>
    src/main.c (locate write_smote_ab_report() and the ABResult struct by name — line numbers have shifted since Plan 03-02)
    include/metrics.h (metrics_mcnemar signature)
    include/config.h (FEATURES_PER_VOWEL, NUM_METADATA_FEATURES)
    SPEC.md lines 123-131 (verbatim acceptance criterion)
  </read_first>
  <action>
Implement `static void write_gap1_report(const ABResult *off_res, const ABResult *on_res, const
char *report_path, const char *csv_path)` immediately after `write_smote_ab_report()` (same file
region as the other `write_*_report()` functions), mirroring its exact structure: open
`report_path`, write a header line naming the comparison ("SELECAO PARACONSISTENTE (Gap 1): SEM
vs COM selecao" or equivalent), write the same per-metric `%-28s %.4f [%.4f, %.4f] ... %.4f
[%.4f, %.4f]` CI table (accuracy, macro_f1, then the 5 per-class F1 rows using
`CLASS_NAME_NORMAL`/`CLASS_NAME_LARYNGITIS`/`CLASS_NAME_DYSPHONIA`/`CLASS_NAME_FUNC_DYSPHONIA`/
`CLASS_NAME_REINKE`, same as `write_smote_ab_report()`'s existing 7-row block) sourced from
`off_res->ci[...]`/`on_res->ci[...]`.

Compute the direct McNemar comparison between the two arms exactly like
`write_smote_ab_report()`'s existing call: `float chi2_ab, p_ab; metrics_mcnemar(off_res->y_true,
on_res->y_pred, off_res->y_pred, off_res->n, &chi2_ab, &p_ab);` and print it with the same
"estatisticamente significativa (p<0.05)" / "nao estatisticamente significativa (p>=0.05)"
phrasing already used elsewhere in this file.

Compute `float feature_reduction = 1.0f - (on_res->mean_n_selected / (float)(FEATURES_PER_VOWEL +
NUM_METADATA_FEATURES));` and print it (`%.1f%%` of the total `nf_vowel` width). Implement
SPEC.md's exact 3-branch rule as fixed `if`/`else if`/`else` (never narrative, never a comment
asserting the outcome without the code enforcing it):
- `if (on_res->macro_f1 >= off_res->macro_f1)`: `fprintf(f, "DECISAO: Selecao Paraconsistente
  ADOTADA (Macro F1 com-selecao=%.4f >= sem-selecao=%.4f, delta=%+.4f, reducao de features=%.1f%%,
  McNemar chi2=%.4f p=%.4f)\n", ...)`.
- `else if ((off_res->macro_f1 - on_res->macro_f1) <= 0.01f && feature_reduction >= 0.30f)`:
  `fprintf(f, "DECISAO: Selecao Paraconsistente ADOTADA (trade-off SPEC.md: Macro F1 levemente
  inferior, delta=%+.4f, mas reducao de features=%.1f%% >= 30%%, McNemar chi2=%.4f p=%.4f)\n",
  ...)`.
- `else`: `fprintf(f, "DECISAO: Selecao Paraconsistente REJEITADA (Macro F1 sem-selecao=%.4f >
  com-selecao=%.4f, delta=%+.4f, reducao de features=%.1f%% insuficiente para o trade-off do
  SPEC.md, McNemar chi2=%.4f p=%.4f) -- mantendo pipeline sem selecao paraconsistente em
  producao\n", ...)`.

Add a reference line pointing to `results/paraconsistent_selection_freq.csv` for the full
per-feature mu/lambda/Gc/Gct/selected table (mirrors `write_smote_ab_report()`'s existing
reference to `smote_borderline_counts_C_baseline.csv`).

Export a parallel CSV at `csv_path`: header `metric,without_selection,without_ci_lower,
without_ci_upper,with_selection,with_ci_lower,with_ci_upper` for the same 7 metric rows
(accuracy, macro_f1, 5 per-class F1), plus 2 trailing summary rows: `mean_n_selected,,,,%f,,`
(only the `with_selection` column populated, using `on_res->mean_n_selected`) and
`feature_reduction,,,,%f,,` (same shape, using the computed `feature_reduction` value).
  </action>
  <acceptance_criteria>
    - `grep -n "DECISAO: Selecao Paraconsistente ADOTADA (Macro F1 com-selecao" src/main.c` present
    - `grep -n "DECISAO: Selecao Paraconsistente ADOTADA (trade-off SPEC.md" src/main.c` present
    - `grep -n "DECISAO: Selecao Paraconsistente REJEITADA" src/main.c` present
    - `grep -c "feature_reduction" src/main.c` outputs at least `2` (computed once, referenced in both the decision logic and the printed output)
    - `grep -c "0.01f && feature_reduction >= 0.30f" src/main.c` outputs `1` (SPEC.md's exact trade-off thresholds, hardcoded as the literal rule, not a config constant that could silently drift — matches SPEC.md's own literal "-0.01"/"30%" wording)
    - `make 2>&1 | tee /tmp/build_03_03_t1.log; grep -c "error:" /tmp/build_03_03_t1.log` outputs `0`
  </acceptance_criteria>
  <verify>
    <automated>make 2>&1 | tee /tmp/build_03_03_t1.log; test "$(grep -c 'error:' /tmp/build_03_03_t1.log)" = "0"</automated>
  </verify>
  <done>write_gap1_report() implements SPEC.md's exact 3-branch Gap 1 acceptance rule as fixed code logic, mirrors write_smote_ab_report()'s CI-table/McNemar/CSV structure, and reports mean_n_selected/feature_reduction alongside the DECISAO sentence.</done>
</task>

<task type="auto">
  <name>Task 2: write_gap_adoption_status() (CROSS-01/CROSS-02) + mode_paraconsistent_ab() CLI + dispatch registration</name>
  <files>src/main.c</files>
  <read_first>
    src/main.c (locate mode_smote_ab(), write_arch_compare_report(), and main()'s dispatch chain by name)
    CLAUDE.md (existing "Gap 2 Outcome"/"Gap 3 Outcome" sections — the exact historical numbers to hardcode)
  </read_first>
  <action>
Implement `static void write_gap_adoption_status(const char *decision_gap1, float
gap1_macro_f1_delta, float gap1_mcnemar_p, const char *path)` (place it immediately after
`write_gap1_report()`): open `path` (`results/gap_adoption_status.csv`), write header
`gap,decision,macro_f1_delta,mcnemar_p,citation_status`, then exactly 3 rows:
- Row 1 (Gap 2, hardcoded historical fact, do NOT recompute): `Gap 2 (Borderline-SMOTE),ADOTADO,
  +0.0235,0.6606,Han/Wang/Mao 2005 (Borderline-SMOTE1)`.
- Row 2 (Gap 3, hardcoded historical fact): `Gap 3 (Config C 2-hidden-layer),ADOTADO (sem
  mudanca em config.h),n/a (ja em producao),0.7463,N/A - comparacao metodologica interna`.
- Row 3 (Gap 1, LIVE values from this function's parameters): `fprintf(f,
  "Gap 1 (Selecao Paraconsistente LPA2v),%s,%+.4f,%.4f,%s\n", decision_gap1, gap1_macro_f1_delta,
  gap1_mcnemar_p, citation_str)` where `citation_str` is computed by `if
  (strncmp(decision_gap1, "ADOTAD", 6) == 0) citation_str = "Da Costa 1990 / Abe & Nakamatsu 2009
  (PAL2v)"; else citation_str = "N/A - tecnica nao ativa no modelo final (CROSS-02)";` — this
  `if`/`else` on the actual `decision_gap1` string IS the CROSS-02 enforcement mechanism; never
  hardcode the citation independent of the outcome.

Implement `static int mode_paraconsistent_ab(const char *base_dir)` immediately after
`write_gap_adoption_status()`, mirroring `mode_smote_ab()`'s exact structure: `log_info("=== MODO:
A/B SELECAO PARACONSISTENTE (Gap 1) ===");` `log_info("Atencao: modo de longa duracao (~60-180
min) -- executa o pipeline hierarquico completo duas vezes (sem e com selecao paraconsistente),
fixo em Borderline-SMOTE1 + Config C + regularizacao baseline (configuracao ja adotada nas Fases
1-2)");` declare `ABResult res_off = {0}, res_on = {0};` call `mode_train_ex(base_dir,
SMOTE_BORDERLINE, &ARCH_CONFIGS[2], REG_BASELINE, PARA_SELECT_OFF, &res_off)` then
`mode_train_ex(base_dir, SMOTE_BORDERLINE, &ARCH_CONFIGS[2], REG_BASELINE, PARA_SELECT_ON,
&res_on)` (both fixed at the same SMOTE/arch/reg — RESEARCH.md's Pitfall 5, never call
`mode_train()` or rely on its default). Derive the same `feature_reduction`/decision-branch logic
Task 1 already implemented (or, simpler and less duplicative: have `write_gap1_report()` RETURN
the decision string and McNemar chi2/p — change its signature to `static const char
*write_gap1_report(...)` returning a static/caller-owned buffer, or add out-parameters `char
*decision_out, float *chi2_out, float *p_out` — pick whichever keeps Task 1's function
self-contained while letting `mode_paraconsistent_ab()` pass the real decision through to
`write_gap_adoption_status()` without recomputing the rule a second time; do NOT duplicate the
3-branch if/else — one fixed decision computation, consumed by both the text report and the
adoption-status table). Call `write_gap1_report(&res_off, &res_on,
"results/train_log_v34_gap1_paraconsistent_ab.txt", "results/paraconsistent_ab_comparison.csv")`
then `write_gap_adoption_status(<decision string from write_gap1_report>, res_on.macro_f1 -
res_off.macro_f1, <mcnemar p from write_gap1_report>, "results/gap_adoption_status.csv")`. Free
`res_off.y_true`/`res_off.y_pred`/`res_on.y_true`/`res_on.y_pred`.

Register the mode in `main()`'s dispatch chain, immediately after the existing `arch-compare`
line, same style: `if (strcmp(mode, "paraconsistent-ab") == 0) return
mode_paraconsistent_ab(base_dir) == 0 ? 0 : 1;`.
  </action>
  <acceptance_criteria>
    - `grep -n "\"paraconsistent-ab\"" src/main.c` found inside `main()`'s dispatch chain
    - `grep -n "SMOTE_BORDERLINE, &ARCH_CONFIGS\[2\], REG_BASELINE, PARA_SELECT_OFF, &res_off" src/main.c` (or equivalent exact call) present
    - `grep -n "SMOTE_BORDERLINE, &ARCH_CONFIGS\[2\], REG_BASELINE, PARA_SELECT_ON, &res_on" src/main.c` (or equivalent exact call) present
    - `grep -c "strncmp(decision_gap1" src/main.c` outputs at least `1` (citation status computed conditionally on the live decision, not hardcoded)
    - `make 2>&1 | tee /tmp/build_03_03_t2.log; grep -c "error:" /tmp/build_03_03_t2.log` outputs `0`
    - `./build/vocal_detect 2>&1 | head -1` still prints the usage line (regression check: dispatch chain intact, argc<2 path unaffected)
  </acceptance_criteria>
  <verify>
    <automated>make 2>&1 | tee /tmp/build_03_03_t2.log; test "$(grep -c 'error:' /tmp/build_03_03_t2.log)" = "0" && ./build/vocal_detect 2>&1 | head -1</automated>
  </verify>
  <done>write_gap_adoption_status() produces the 3-row CROSS-01 table with Gap 1's citation field branching on the live decision (CROSS-02); mode_paraconsistent_ab() runs both arms fixed at the already-adopted Gap 2/Gap 3 configuration and is dispatchable via ./build/vocal_detect paraconsistent-ab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `write_gap1_report()`'s decision output -> `write_gap_adoption_status()`'s citation field | The single most important boundary in this plan: if these two functions computed the adoption decision independently (rather than one being the sole source of truth), they could drift and CROSS-02 could be silently violated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering (report integrity, citation-vs-reality mismatch) | `write_gap_adoption_status()`'s Gap 1 citation field | mitigate | Citation string computed by `if`/`else` on the live `decision_gap1` outcome (itself sourced from `write_gap1_report()`'s single fixed decision computation, never recomputed a second time) — directly enforces CROSS-02, the exact risk RESEARCH.md's Pitfall 5/Summary section flags as "already made once with Borderline-SMOTE" |
| T-03-08 | Repudiation (comparison isolates the wrong variable) | `mode_paraconsistent_ab()`'s two `mode_train_ex()` calls | mitigate | Both arms pass identical `SMOTE_BORDERLINE`/`ARCH_CONFIGS[2]`/`REG_BASELINE`, varying only `para_mode` — guarantees the A/B comparison isolates exactly one variable, matching the same discipline `mode_smote_ab()`/`mode_arch_compare()` already established |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — zero external package installs in this phase |

</threat_model>

<verification>
`make` compiles cleanly (0 errors, no new warnings) after each task. Task 1's grep-based acceptance criteria confirm all 3 DECISAO branches exist as fixed code. Task 2's grep-based acceptance criteria confirm the CLI dispatch registration and the CROSS-02-enforcing citation logic. No pipeline run happens in this plan — that is Plan 03-04's job.
</verification>

<success_criteria>
- Gap 1's adopt/reject decision is computed by fixed 3-branch code logic implementing SPEC.md's exact trade-off rule, never asserted by inspection (PARA-05).
- The consolidated Gap Adoption Status table exists with 3 rows, Gap 1's citation status computed conditionally on the live decision (CROSS-01, CROSS-02).
- `./build/vocal_detect paraconsistent-ab .` is dispatchable and, when run, will exercise both arms under the already-adopted Gap 2/Gap 3 configuration.
</success_criteria>

<output>
Create `.planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-03-SUMMARY.md` when done
</output>
