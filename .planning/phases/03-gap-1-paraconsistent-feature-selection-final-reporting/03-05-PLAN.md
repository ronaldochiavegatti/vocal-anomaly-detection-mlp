---
phase: 03-gap-1-paraconsistent-feature-selection-final-reporting
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified: [CLAUDE.md]
autonomous: true
requirements: [PARA-06, CROSS-01, CROSS-02]

must_haves:
  truths:
    - "CLAUDE.md accurately reflects the real Gap 1 outcome from Plan 03-04's executed run -- every numeric claim traces to a grep-able source in results/train_log_v34_gap1_paraconsistent_ab.txt or results/paraconsistent_ab_comparison.csv, no invented numbers"
    - "CLAUDE.md's Feature Count table matches config.h's actual current values (251 total / 83 per vowel / 55 spectral including the 4 glottal-source features), not the stale 237/79/51"
    - "The Gap Adoption Status table in CLAUDE.md matches results/gap_adoption_status.csv exactly (3 rows, same decision/delta/mcnemar-p/citation strings)"
    - "CLAUDE.md explicitly discloses that mode_train()'s default CLI path (train/full) is unaffected by Gap 1 -- it remains PARA_SELECT_OFF for regression-safety reasons, same as it remained SMOTE_STANDARD after Gap 2"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected Feature Count table + new Gap 1 Outcome subsection + Gap Adoption Status (Milestone Closure) section"
      contains: "Gap 1 Outcome"
  key_links:
    - from: "CLAUDE.md's Gap 1 Outcome subsection"
      to: "results/train_log_v34_gap1_paraconsistent_ab.txt"
      via: "verbatim-quoted DECISAO sentence and comparison numbers"
      pattern: "train_log_v34_gap1_paraconsistent_ab.txt"
---

<objective>
Document the real Gap 1 (paraconsistent feature selection) outcome in `CLAUDE.md` — the DECISAO sentence, the mu/lambda formula substitution rationale, the selection-frequency findings — regardless of which outcome fired, correct the stale Feature Count table (237/79/51 -> 251/83/55, including the 4 previously-undocumented glottal-source features), and add the final consolidated Gap Adoption Status table closing the milestone.

Purpose: PARA-06 explicitly requires this update "independente do resultado" (regardless of outcome), directly mirroring Phase 1's SMOTE-05 (`01-04-PLAN.md`) and Phase 2's ARCH-06 (`02-04-PLAN.md`) precedents. This is also the last plan in the milestone — CROSS-01/CROSS-02 close here with a real, evidence-backed 3-gap summary ready for the PIBIC committee.
Output: `CLAUDE.md` updated with a corrected Feature Count table, a new dated Gap 1 Outcome subsection, and a Gap Adoption Status (Milestone Closure) section; no other file touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-RESEARCH.md
@.planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-04-SUMMARY.md
@.planning/phases/01-gap-2-borderline-smote/01-04-PLAN.md
@.planning/phases/02-gap-3-shallow-vs-deep-mlp-comparison-with-structural-refacto/02-04-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the Feature Count table + add the Gap 1 Outcome subsection</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (full file — short, read in full)
    .planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-04-SUMMARY.md (the real verdict: off_res/on_res numbers, DECISAO branch, mean_n_selected/feature_reduction, 0.42-floor check)
    include/config.h (authoritative current feature counts: NUM_SPECTRAL_FEATURES=55, FEATURES_PER_VOWEL=83, TOTAL_FEATURES=251)
    results/train_log_v34_gap1_paraconsistent_ab.txt (source of every number quoted in the new subsection — quote verbatim, do not paraphrase numbers)
  </read_first>
  <action>
In `CLAUDE.md`'s `### Feature Count (config.h)` table, correct every stale value against the real
`include/config.h` (verified directly this session): `NUM_SPECTRAL_FEATURES` row `51 -> 55` with
an added note that this includes 4 glottal-source features (Oq/Sq/NAQ/H1-H2) not previously
listed in this table; the "Per vowel" row `79 -> 83`; the "Total (3 vowels)" row `237 -> 251`.
Update the "After selection" row: replace the old placeholder `~150-190` with the real range
observed in this phase's `results/paraconsistent_selection_freq.csv`/`03-04-SUMMARY.md`
(`mean_n_selected` for Master and Expert, out of 85 per-vowel features) — use the actual number,
not an invented range.

Add a new `## Gap 1 Outcome — Paraconsistent Feature Selection (LPA2v, v34)` subsection,
immediately after the existing `## Gap 3 Outcome` subsection (matching that subsection's and `##
Gap 2 Outcome`'s exact format: verbatim DECISAO quote, comparison table, files-produced list).
Content, sourced ONLY from `03-04-SUMMARY.md`/`results/train_log_v34_gap1_paraconsistent_ab.txt`
(never invented):
1. The verbatim `DECISAO: Selecao Paraconsistente ...` sentence, quoted exactly as printed.
2. A comparison table: without-selection vs with-selection Macro F1 (+ 95% CI), per-class F1,
   `mean_n_selected`, `feature_reduction` — sourced from `results/paraconsistent_ab_comparison.csv`.
3. The direct McNemar chi2/p between the two arms.
4. An explicit statement of the mu/lambda formula substitution and why: mu = one-way ANOVA
   eta-squared (SSB/SST, naturally bounded [0,1]), NOT SPEC.md's non-standard per-class Fisher
   ratio; lambda = unweighted mean of per-class sigma/global-sigma ratio, NOT SPEC.md's `CV =
   std/mean` (which explodes for near-zero-mean delta-MFCC features, an issue already documented
   elsewhere in this file for a different reason). Note this is a documented, deliberate deviation
   from SPEC.md's literal pseudocode (not an oversight, not a silent change), and flag that the
   exact lambda formula is a researcher-synthesized substitute (RESEARCH.md Assumptions Log A1) —
   the domain-specific precedent papers (Costa et al. 2019 DPM; a 2025 wavelet+paraconsistent
   paper; a 2021 grid-fault paper) remain paywalled, so a future update with institutional access
   may revise this formula.
5. The CLAUDE.md 0.42 Macro F1 regression-floor check result (pass, or the documented exception),
   carried forward from `03-04-SUMMARY.md` — do not omit this even if the floor check passed
   trivially.
6. File pointers: `results/train_log_v34_gap1_paraconsistent_ab.txt`,
   `results/paraconsistent_ab_comparison.csv`, `results/paraconsistent_selection_freq.csv`.

Do not alter any other section of `CLAUDE.md` in this task besides the Feature Count table and
this new subsection (Gap 2 Outcome / Gap 3 Outcome / Build & Run Commands / Architecture stay
untouched).
  </action>
  <acceptance_criteria>
    - `grep -n "251" CLAUDE.md` shows the corrected `TOTAL_FEATURES`/feature-count table entries
    - `grep -c "## Gap 1 Outcome" CLAUDE.md` outputs `1`
    - `grep -c "DECISAO: Selecao Paraconsistente" CLAUDE.md` outputs at least `1` (verbatim DECISAO quoted)
    - `grep -c "eta-squared\|eta2\|ANOVA" CLAUDE.md` outputs at least `1` (mu formula substitution documented)
    - `grep -c "0.42" CLAUDE.md` outputs at least `1` in the vicinity of the new subsection (regression floor check documented — this constant already appears elsewhere in CLAUDE.md's Constraints section, so also manually confirm the NEW occurrence is inside the Gap 1 Outcome subsection, not just a false-positive match on the pre-existing one)
    - Every numeric value in the new subsection is traceable: spot-check at least 3 numbers (Macro F1 delta, mean_n_selected, McNemar p) against `results/train_log_v34_gap1_paraconsistent_ab.txt`/`results/paraconsistent_ab_comparison.csv` via `grep`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "## Gap 1 Outcome" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md's Feature Count table matches config.h's real current values; a new Gap 1 Outcome subsection exists with the verbatim DECISAO sentence, the comparison numbers, the mu/lambda formula substitution rationale, and the regression-floor check result, all traceable to Plan 03-04's real executed run.</done>
</task>

<task type="auto">
  <name>Task 2: Gap Adoption Status (Milestone Closure) section + explicit mode_train() default disclosure</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (post Task 1 edit)
    results/gap_adoption_status.csv (the real 3-row table)
    .planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-RESEARCH.md (Open Question 2 — the mode_train() default-disclosure requirement)
  </read_first>
  <action>
Add a `## Gap Adoption Status (Milestone Closure)` section to `CLAUDE.md` immediately after the
new `## Gap 1 Outcome` subsection, reproducing `results/gap_adoption_status.csv`'s 3 rows as a
markdown table with columns `Gap | Decision | Macro F1 delta | McNemar p | Citation status` —
copy every field verbatim from the CSV, do not reword or recompute any value.

Immediately below that table, add an explicit closing disclosure paragraph (never silently left
ambiguous, per RESEARCH.md's Open Question 2): state plainly that `mode_train()`/`make train`/
`make full`'s default CLI path remains on `SMOTE_STANDARD` + `PARA_SELECT_OFF` + Config C at
baseline regularization for regression-safety reasons — this was an explicit Phase 1 decision
(documented in `STATE.md`), unchanged by Phase 2 or this phase — and that the actually-adopted
configuration per gap (Borderline-SMOTE if Gap 2's row says ADOTADO, paraconsistent selection if
Gap 1's row says ADOTADO) is only reachable via the dedicated comparison CLI modes (`smote-ab`,
`arch-compare`, `paraconsistent-ab`), never via the plain `train`/`full` entry point. State this
as the CROSS-02-mandated disclosure that prevents a future citation/reality mismatch (e.g., a
poster or report claiming "production uses Borderline-SMOTE + paraconsistent selection" while
`make train` still runs the pre-Gap-2/Gap-1 configuration).

Update the milestone-level status indicator wherever `CLAUDE.md` currently states project status
(near "Current Best Results" or the project header) to note all 3 SPEC.md gaps are now closed
with explicit, evidence-backed adopt/reject outcomes (do not claim a Macro F1 number here beyond
what is already stated in the per-gap outcome subsections — this is a status/closure statement,
not a new metrics claim).
  </action>
  <acceptance_criteria>
    - `grep -n "## Gap Adoption Status" CLAUDE.md` exists with a markdown table containing 3 data rows (Gap 1, Gap 2, Gap 3)
    - `grep -n "PARA_SELECT_OFF" CLAUDE.md` and `grep -n "SMOTE_STANDARD" CLAUDE.md` both present near the disclosure paragraph
    - Manually confirm each of the 3 table rows' 5 fields (gap/decision/delta/mcnemar-p/citation) match `results/gap_adoption_status.csv` exactly, field-for-field
    - `git diff --stat CLAUDE.md` shows only additions/corrections to the Feature Count table, the Gap 1 Outcome subsection, and the Gap Adoption Status section — no unrelated content removed
  </acceptance_criteria>
  <verify>
    <automated>grep -c "## Gap Adoption Status" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md carries the final 3-row Gap Adoption Status table (matching results/gap_adoption_status.csv exactly) and an explicit disclosure that mode_train()'s default CLI path is unaffected by any of the 3 gaps' adoption decisions, closing the milestone's CROSS-01/CROSS-02 requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documentation content -> future readers (PIBIC committee, future contributors, future Claude sessions) | Inaccurate documentation here directly undermines the "comprovavel por comparacao A/B" core value this entire milestone exists to establish — this is the LAST plan in the milestone, so any error here is uncorrected until a future phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Repudiation (documentation drift from reality) | `CLAUDE.md`'s Gap 1 Outcome / Feature Count / Gap Adoption Status sections | mitigate | Every numeric claim is required to trace verbatim to a grep-able source in `results/*.txt`/`results/*.csv` (acceptance criteria enforce spot-checks); the mode_train() default-disclosure paragraph explicitly prevents a CROSS-02 citation/reality mismatch |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — documentation-only plan, zero external packages |

</threat_model>

<verification>
`grep -c "## Gap 1 Outcome" CLAUDE.md` == 1. `grep -c "## Gap Adoption Status" CLAUDE.md` == 1. The Feature Count table shows 251/83/55, not 237/79/51. The mode_train() default-CLI disclosure paragraph is present. `git diff CLAUDE.md` touches only the intended sections.
</verification>

<success_criteria>
- CLAUDE.md's Gap 1 claim is accurate and evidence-backed, regardless of which outcome (adopt/reject) was reached.
- CLAUDE.md's Feature Count table matches config.h's real current values.
- A consolidated, CSV-matching Gap Adoption Status table closes the milestone, with an explicit mode_train()-default disclosure satisfying CROSS-01/CROSS-02.
</success_criteria>

<output>
Create `.planning/phases/03-gap-1-paraconsistent-feature-selection-final-reporting/03-05-SUMMARY.md` when done
</output>
