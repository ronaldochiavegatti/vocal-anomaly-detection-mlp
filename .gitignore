# Build artifacts
build/
*.o

# Trained model weights
models/

# Audio datasets (SVD - Saarbrücken Voice Database)
# Treated/processed WAV files from SVD not included in repository.
# Directory structure expected:
#   saudavel/          - healthy voice samples (SVD class: normal)
#   laringite/         - laryngitis samples (SVD class: laryngitis)
#   disfonia_psicogênica/ - psychogenic dysphonia samples (SVD class: dysphonia)
saudavel/
laringite/
disfonia_psicogênica/

# Feature cache and results (generated by pipeline)
results/features.csv
results/extract_log.txt

# Stale/empty planning files
plan.md
plan2.md
projectmemory.md
lastcld.md

# Claude internal state
.claude/

# Object files in root (legacy, should be in build/)
*.o
